# ASM-PR6 — kế hoạch triển khai mỏng cho `supabase-safe-migration`

## Trạng thái và nguồn thẩm quyền

**Current end-of-pass ngày 2026-09-05:** đã chạy hết pending eligible work của revision 2 theo continuation contract, không dùng semantic canary gate. Lượt sau quota dispatch đúng `0 reader + 10 evaluator`, reuse `46`, invalidation `0`, exit `1/incomplete`. Final `56 succeeded / 6 outcome_unknown / 4 dependency_blocked`, pending/running/failed/integrity/budget-blocked `0`. Report `16 current / 6 incomplete`; main adjudication trên 16 complete graphs: `8 passed / 7 partially_passed / 1 failed`. Không acceptance; sáu incomplete cases không có case/comparison verdict. Xem [end-of-pass report](#continuation-end-of-pass--2026-09-05). Các entries bên dưới là lịch sử trước report này.

**Resume sau quota ngày 2026-09-05:** owner yêu cầu “tiếp tục phần còn lại nha, quota hồi rồi”. Ngày 2026-09-04 đã pause ngay theo yêu cầu hết quota, interrupt coordinator, xác nhận không còn worker của run; persisted snapshot lúc pause: `46 succeeded / 4 outcome_unknown / 2 running-unreconciled / 10 pending / 4 dependency_blocked`, tổng 54 started attempts kể cả 5 historical. Hai evaluator bị interrupt chưa có result được giữ evidence và reconcile bởi same-run resume, không giả success. Pre-resume ngày 2026-09-05 kiểm tra snapshot `23/23`, plan hash, runtime hash, skill/suite/harness unchanged và không có worker cũ; tiếp tục đúng 10 pending evaluators với concurrency 2. Không retry 4 reader unknown hoặc 2 evaluator interrupted, không prepare revision mới/reset budget. Final report còn chờ command settle. Các continuation entries phía dưới giữ lịch sử.

**Continuation đang thực hiện ngày 2026-09-04:** owner yêu cầu “đọc contract hiện tại và thực thi theo nó” sau contract checkpoint `f663f349d9b817f3eb7ef03b73ba8f3b50874c60`. Tiếp tục đúng run revision 2/B/C_fix_1, tối đa remaining `42 readers + 21 evaluators = 63` new dispatch, concurrency 2, không automatic retry/correction/new run/commit. Pre-resume xác nhận snapshot `23/23`, execution-plan hash và skill/harness/suite unchanged. Lần resume trong sandbox gặp network `os error 10013`; operator interrupt coordinator để tránh tiêu tốn cả wave, giữ 4 started reader attempts (2 timeout unknown, 2 interrupted cần reconcile). Read-only process check xác nhận không còn worker của run; network check ngoài sandbox tới API:443 thành công. Resume cùng run với network permission phù hợp, giữ unknown attempts và tiếp tục pending độc lập. Final counts/verdict chờ command settle; không gọi interruption là clean completion hoặc giả response chưa có. Evidence ở transient CP0 folder, prefix `continuation-`.

**Workflow revision theo owner ngày 2026-09-04:** bỏ semantic-pass gate của canary. Mặc định chạy hết work còn runnable; unit hết attempt hoặc chưa semantic pass được ghi pending resolution, không chặn các unit độc lập. Sau lượt đó tổng hợp evidence/findings/phương án correction rồi chạy lại đúng affected scope. Có thể pause sớm để điều tra nhưng phải giữ run/revision/attempts và có đường resume, không khởi động lại toàn bộ evaluation. Đây là thay đổi execution procedure; không rewrite lịch sử canary, không cấp thêm live/commit/DB action từ việc cập nhật docs. Exact continuation/correction contract nằm trong mục “Exact evaluation, reuse và correction flow”; các grants/outcomes dưới đây là historical records.

**Current re-canary outcome:** correction implementation đã review pass và committed `C_fix_1 = af732c235d0955f6dafb17718a0cbe34f858b01d`. Same-run revision `2` re-canary dùng đúng `1 candidate reader + 1 evaluator`, baseline reused; execution succeeded nhưng main semantic verdict vẫn `partially_passed / inconclusive`, `0 Critical / 2 Required`. Tổng live calls `5`, retry `0`; candidate/evaluator canary đã hết lifetime ordinal `2/2`. Grant correction/commit/re-canary đã thực hiện; không full wave hoặc call tiếp theo. Các verdict/authority phía dưới là snapshot lịch sử theo từng checkpoint.

**Current correction grant:** owner yêu cầu “correction -> review -> pass thì commit -> re canary -> report”. Authorize đúng hai proposed corrections ở canary ledger: core reporting cho selected/skipped references và migration verification theo full constraint row scope/invalid data. Review đạt mới tạo một correction commit; sau đó same-run prepare B/C_fix_1, reuse baseline và chạy lại đúng canary candidate/evaluator (dự kiến `1 + 1 = 2` new calls, ordinal `2`, retry tự động `0`), rồi report. Grant bao gồm egress corrected candidate package + frozen paired evidence tới cùng Codex/OpenAI runtime; không sửa suite/harness, không full remaining wave, không thêm correction/retry cycle, push hoặc database action. Đây là material reporting clarification đã được owner approve; không sửa historical canary verdict.

**Current canary outcome:** exact owner-authorized live canary đã chạy `2 readers + 1 evaluator = 3` calls, cả ba execution `succeeded`, retry `0`. Main semantic adjudication: candidate `partially_passed`, comparison `inconclusive`, không confirmed candidate safety veto nhưng còn `2 Required` evidence omissions. Dừng trước remaining `63` theo canary gate; CP3/CP4 chưa hoàn tất. Không skill correction hoặc rerun được thực hiện; các static preparation readiness statements dưới đây không thay semantic verdict này.

**Current CP3 preparation:** CP2 đã committed tại `C_eval = 3def69126e8f8775ea17a21e3c3667a67e6c8263` theo owner “duyệt”; grant đã consumed. B/C_eval prepare và package audit hoàn tất, run `run-d2e1b1d1bcee4334ab28374cf6549b1b`, revision `1`, dispatch `0`. Chờ exact live/egress grant; chưa comparative observation hoặc acceptance. Các entries trước prepare bên dưới là historical snapshots, không standing authority.

Owner instruction ngày `2026-09-04`: “thực hiện detail plan asm pr6” duyệt detailed plan hiện tại và cấp local implementation theo đúng checkpoints. CP0 hoàn tất; CP1 committed tại `C_struct = 871dabcb34ab6125ccb75a6d2f2ced523a748c54` sau owner đồng ý local commit. CP2 implemented/verified, chưa commit; CP3/CP4 chưa thực hiện. Grant commit CP1 đã consumed, không cấp commit CP2 hoặc live/model/egress/retry, push, PR, CI, merge và database actions. Những trạng thái cũ trong evidence planning/CP1 là lịch sử trước checkpoint hiện tại.

- [Master](../../plan.md) và [roadmap](../../structural-migration-roadmap.md) sở hữu program scope/order/invariants; [progress](../../progress.md) sở hữu actual status; [owner brief](./owner-review-brief.md) ghi quyết định PR6.
- [CLI master](../eval-harness-cli-first/plan.md), [Stage 3](../eval-harness-cli-first/stage-3-cli-reuse.md), [Stage 4](../eval-harness-cli-first/stage-4-cli-evaluator-report.md) và [operator guide](../../eval-design.md) sở hữu execution/reuse/report procedure hiện hành. PR6 không thiết kế lại harness.
- [Stage 4 closure/pilot evidence](../../progress.md#7-stage-4-tooling-closure-và-migration-handoff-2026-09-04) là migration handoff, không phải semantic acceptance.
- Đọc `AGENTS.md`, `docs/agent-loops.md`, các skill `implementation-planning-and-pr-breakdown`, `maintain-repo-skills`, `supabase-safe-migration`, `test-quality-strategy`, `git-checkpoint-workflow`; dùng `code-review-and-quality` cho checkpoint/correction/cumulative review. References đã áp dụng khi lập kế hoạch: planning tracked-program/handoff; governance progressive-disclosure/fresh-reader/eval-design; Git branch-start; review domain dimensions.

Sizing: preliminary `large/high-risk` do reconciliation nhiều owner; final là một PR có phạm vi skill hẹp nhưng acceptance nhạy cảm về DB/permission. Giữ một plan mỏng, checkpoints tuần tự, `0 specialist`; không có residual planning uncertainty cần delegation. Manual semantic review về sau là bắt buộc; không phải database QA.

## Baseline đã xác minh

`B = 2be02df11e279b5c88f37d2fd609069a54c235ed`, exact refreshed `main == origin/main`, PR #81 merge. Nhánh `refactor/agent-skills-asm-pr6-supabase` tạo từ B, không stacked. Preflight tree/index sạch; local main từ `69e7235` fast-forward 12 commits, ahead/behind `0/0`. Git xác nhận các ancestors:

- ASM-PR5B / PR #73: `effb5571955aa09b714e97b7162a6bb3bed0bca4`.
- Corrected DB coverage: `35cc5a115751412b43253cd100ba6a8df5503af0`.
- Stage 4 closure head: `721030d9d772ff190354d61afc02de29edf40a55`.

| Artifact tại B | Git blob/tree |
| --- | --- |
| `.agents/skills/supabase-safe-migration/SKILL.md` | `a2ae145d2e4cc77a66e5fa9bde898028ca02458f` |
| `.agents/evals/supabase-safe-migration/regression.json` | `34e4e566a0e06838ed56d0e1dca54ac266cde07c` |
| `.agents/evals/supabase-safe-migration/routing.json` | `a67bd579bb3853a11bd77f7a70cd8d7bc31d687d` |
| `.agents/evals/supabase-safe-migration/fresh-reader.json` | `36a2b40826becdb4e1542b309cf31b30d58ca4af` |
| `docs/adr/refactor-exercise-authoring.md` | `98c9cb3cc13bbec2defb245fcdc41d333a469b24` |
| `.agents/scripts` | tree `c8f0e97c57bbb743b80d39e9708792eefdc9aa55` |

Baseline **skill bytes** tại B giống ASM-PR5B merge; suite regression và ADR đã đổi. Dùng B làm immutable baseline source, không dùng old evaluator output hoặc pilot làm accepted baseline. Baseline được giữ để so sánh ngay cả khi có defect; không sửa B, không gọi baseline là semantic pass.

Control plane của mọi comparison có provenance từ đúng ba suites tại B, toàn bộ `executor_input.context`/inline text và `evaluator_only` trong chúng. Inventory là ba suite files cộng union các `repository_file` context được derive từ ba suite JSON tại B: hiện có `3 + 20 = 23` files, không chỉ ADR trong bảng. Không thêm related skill prose, planner narrative, expected answer hay reference hint vào reader package. `AGENTS.md` và các SQL/tests/actions/schema/config/seed được suite chọn chỉ là read-only context.

**Pinning gồm hai lớp, không thay thế nhau:**

1. **Provenance với B:** đọc Git blob bằng byte buffer; với từng suite/context, so nội dung sau phép chuẩn hóa duy nhất `CRLF → LF` ở cả working-tree bytes và blob bytes. Không trim whitespace, bỏ BOM, đổi Unicode hoặc parse/reserialize JSON để vượt kiểm tra này. Git blob IDs trong bảng là provenance, không phải SHA-256 của bytes được capture. CRLF/LF-only difference hợp lệ tại admission; mọi khác biệt khác phải dừng và reconcile.
2. **Exact evaluation bytes tại CP0:** sau provenance check, freeze một snapshot ngoài repo với exact sorted path inventory, Git blob ID tại B và SHA-256 + byte count của **raw working-tree bytes** cho đủ 23 files. Record snapshot location/hash trong CP0 evidence ledger để fresh session dùng lại; đây là operator evidence, không thêm harness schema/tool. Trước và sau mọi prepare, kể cả correction/same-ref revision, kiểm tra inventory và raw SHA-256/byte count khớp snapshot CP0. Không chuẩn hóa EOL ở bước này: đổi CRLF/LF sau freeze cũng là drift, phải dừng trước dispatch, không âm thầm re-freeze. Snapshot thiếu/mất thì chưa đủ admission/evidence cho continuation; không tạo hash thay thế từ Git blobs hoặc rehearsal cũ.

**Source detail và kiểm tra package:** `prepareSkillEvalWorkspace` đọc suites từ checkout đang chạy; `prepareSyntheticWorkspace` capture raw context bằng `readFileSync` cho cả hai variants. `--baseline-ref`/`--candidate-ref` chỉ pin skill bundles, không pin control-plane bytes. Sau prepare, đối chiếu exact inventory/path/SHA-256/byte count trong `workspace-manifest.json.control_plane.files` với snapshot CP0; đối chiếu từng copied repository context và `execution-context-manifest.json.context` entry của cả hai variants với hash nguồn tương ứng. Suite files dưới `evaluator/suite-definitions` được harness canonicalize: kiểm tra bytes bằng `canonicalJson` của parsed frozen suite và hash trong `artifact_inventory`, không yêu cầu raw suite copy giống CRLF nguồn. Prompt/inline context/policy/rubric phải derive từ cùng frozen suite; giữ existing compilation/validation, không đổi harness. Workspace/revision/provenance identities có thể đổi, nên không yêu cầu toàn bộ manifest hay `execution_context_hash` giống nhau giữa revisions. Fresh session trên checkout có EOL khác vẫn phải đáp ứng exact snapshot CP0 để tiếp tục cùng comparison; normalized provenance pass riêng lẻ không đủ.

## Reconcile roadmap và handoff

| Assumption cũ | Kết luận và procedure PR6 |
| --- | --- |
| Lấy baseline từ ASM-PR5B merge | Dependency vẫn đúng; execution snapshot đã stale. Pin B với corrected suite/ADR và current CLI; skill baseline không đổi bytes. |
| Runner chỉ prepare, cần manual execution ngoài runner | Chỉ đúng với v1 lịch sử. CLI-first hiện prepare → reader/evaluator waves → advisory report; không dùng App Server/CP9 hoặc Stage 1 `execute-prepared` làm workflow PR6. |
| First migration structural-only, đồng thời backlog yêu cầu thêm behavior | Giữ structural-only ở checkpoint CP1; CP2 riêng chứa đúng semantic addition đã ghi trong handoff. Không trộn rewrite vào verbatim move, không mô tả final candidate là pure equivalence. |
| Mỗi correction phải chạy lại toàn bộ baseline/candidate | Dùng same-run exact-success reuse theo fingerprint và dependency. Baseline success không đổi được giữ; bundle edit thường invalidate toàn bộ candidate readers. |
| Sửa 1–2 dòng chỉ ảnh hưởng vài case | Sai với full-bundle stdin hiện tại. `patch-check` là selected closure để chẩn đoán; retained evidence không chứng minh toàn revision hiện tại. |
| Report exit `0`, `succeeded` hoặc `current` là accepted migration | Sai. CLI chỉ xác nhận execution/coverage và advisory proposal; main review và owner acceptance là riêng. |
| Immutable comparative report/human writer của workflow cũ là bắt buộc phải chạy qua CLI | CLI `report` chỉ stdout JSON, không ghi v1 `human_evaluation`/`generated_report`, không có `accept` command. Lưu report snapshot/hash bên ngoài repo và ghi reviewer decision riêng; không tự làm bridge/tooling mới. |
| Exact resource-access evidence chứng minh giảm context sau split | CLI nhúng **mọi** bundle file. Có thể kiểm chứng supplied file/hash từ exact stdin, nhưng không có per-resource runtime-read instrumentation hoặc CLI `skill_resource_access` writer. `read=unknown` nếu không có observation đủ tin cậy; không claim supplied/read/token reduction. |
| Manual selected-reference fresh-reader methodology cũ còn là runtime procedure | Giữ independent blind behavioral comparison, matching/near-miss/overlap/stop criteria. Dùng 5 fresh-reader suite cases qua CLI đầy đủ bundle; kết quả chỉ chứng minh lựa chọn procedure trong response, không chứng minh chỉ các reference đó được load. |
| Program fresh-reader default cho phép mọi CLI model call | Default advisory/read-only vẫn thuộc roadmap; Stage 4 exact live grant/egress/budget áp dụng cho CLI. Grant pilot đã consumed; lượt planning này không gọi model. |
| Tooling pilot đã giải quyết semantic veto | Không. Corrected pilot `3 reader + 3 evaluator` chứng minh transport/reuse/rerun; latest corrected-input run `1 + 1` vẫn `partially_satisfied / triggered`. Không phải A/B hay phép đo model variance. |

Giữ nguyên: nine-PR/six-phase order; PR6 sau ASM-PR5B; chỉ migrate Supabase; ba references và core safety; suites audit-only; safety veto/material inconclusive chặn acceptance, không chặn independent evaluation; independent rollback; không DB mutation; final nine-candidate/two-unsplit reconciliation. Không yêu cầu các wave đã merge chạy lại CLI chỉ để đồng nhất evidence lịch sử.

## Phạm vi và candidate contract

Expected files khi implementation được cấp: `.agents/skills/supabase-safe-migration/SKILL.md`, ba references dưới đây và docs sở hữu PR6/status/program reconciliation. Không sửa suites, ADR, SQL/published migrations, application/tests, skill khác, `AGENTS.md`, lifecycle, `.agents/scripts/**`, CI hoặc dependencies. Coverage/harness defect cần correction riêng được review/authorize trước khi tiếp tục PR6.

| Reference | Read condition giữ nguyên từ roadmap | Nội dung và nhóm được skip |
| --- | --- | --- |
| `references/migration-and-seed.md` | Read before adding/reviewing a migration, schema/table/column/index/constraint/backfill, or seed change | Migration safety, file placement, seed và matching verification; skip khi chỉ điều tra RLS/RPC/trigger/Storage không có migration/seed review/change. |
| `references/rls-and-storage.md` | Read before changing/reviewing RLS policies, permission helpers, bucket access, or Storage policies | RLS/helper và Storage procedure; skip schema-only/RPC-only/trigger-only. |
| `references/rpc-trigger-concurrency.md` | Read before changing/reviewing RPC, trigger, SQL helper, race-sensitive transition, lock, retry, or idempotency behavior | RPC/trigger/concurrency procedure; skip additive schema/index/seed không có các behavior đó. |

Core giữ activation/related routing; migration-as-source, published-history immutability, no RLS/constraint weakening; existing-data safety; remote/db-push permission; inspection và stop/conflict rules; minimum permission/`SECURITY DEFINER`/safe `search_path`/short-lock/no-external-call/idempotency rules; verification truth/reset requirement khi thực sự đổi migration; reporting/checklist/specialist gate. Nếu task overlap, đọc mọi matching reference; mọi link trực tiếp, contained, không nested discovery. CP1 phải lập section move map B → core/reference và chỉ thêm routing/heading glue cần thiết, không chỉnh examples hay lược invariant.

CP2 là semantic delta riêng theo roadmap backlog: thêm minimum rule trong core yêu cầu xác định active display order, deleted-state retention và restore conflict semantics trước ordered soft-delete backfill/constraint decision; kiểm chứng từ ADR/mutations/tests, không gộp các domain nếu product contract chưa cho phép. Chi tiết ở `migration-and-seed.md`: validate dữ liệu trước chọn correction; không tự chọn tie-break cho business order mơ hồ; dừng khi không suy ra được order; bảo toàn deleted metadata theo contract; restore là separately authorized reconciliation mutation với invalid/ambiguous-data/restore-conflict coverage phù hợp. Không hard-code `question_options`, index `0..n-1` hoặc product-specific SQL vào generic skill. Không thêm requirement chạy DB trong skill evaluation.

Candidate identities:

- `C_struct`: immutable checkpoint sau CP1; chỉ structural move, audit trực tiếp với B.
- `C_eval`: immutable checkpoint sau CP2; initial evaluated candidate gồm structural move + đúng semantic delta trên.
- `C_fix_n`: immutable correction checkpoint nếu review/evaluation phát hiện lỗi, luôn so với B và trace delta từ prior candidate.

Tại planning snapshot, các hash candidate chưa tồn tại; C_struct/C_eval/C_fix_1 hiện đã được ghi trong checkpoint ledgers bên dưới. Với mỗi candidate tiếp theo, implementing session phải record full 40-hex ref sau **separate authorized commit**, bundle manifest hash và exact diff trước prepare/live. Không dùng `HEAD` nổi hoặc giả định hash tương lai. Nếu chưa có commit authority thì dừng ở local checkpoint, không tự commit để tạo candidate. Không promote C_struct/C_eval thành baseline mới để che regression. Không cần một live run riêng trên C_struct: verbatim preservation được audit; final behavior so B/C_eval, intentional semantic delta được ghi rõ thay vì claim structural causality từ A/B.

## Checkpoints tuần tự

| CP | Outcome và điều kiện qua checkpoint |
| --- | --- |
| CP0 — admission | Đọc plan/brief/current owner grant; verify B ancestry/core inventory, normalized-EOL provenance và freeze snapshot raw bytes của đủ 23 suite/context files theo contract hai lớp ở trên; audit đủ 22 case, không đổi coverage. Reconcile latest pilot observations với rubric/context/raw evidence. Coverage gap hoặc new semantic decision ngoài CP2 → stop trước sửa skill. |
| CP1 — structural | Move nguyên văn vào ba references, giữ core minimums và exact routing; validator + diff/link/move audit; main review `0 Critical / 0 Required`; record C_struct khi commit được authorize. |
| CP2 — bounded semantic addition | Chỉ requirement tổng quát ở trên, diff riêng từ C_struct; audit không mang product rule/SQL vào skill; validator + main review; record C_eval khi commit được authorize. Đây là exception rõ ràng cho structural-only first checkpoint, không mở wording cleanup. |
| CP3 — comparative evaluation/correction | Prepare kiểm tra đủ static inputs; live chạy hết runnable scope, giữ independent success và tạm skip unit hết budget. Tổng hợp observations/findings sau lượt; correction hẹp → review → new revision → affected rerun/reuse trong quyền đã cấp. Có thể pause với đủ evidence và exact resume path; semantic failure không chặn remaining cases. |
| CP4 — acceptance và final reconciliation | Đủ 22 coherent current per-case graphs cho final candidate qua report/coverage ledger theo continuation contract; review mọi paired observation/veto/difference; cumulative B..final candidate và correction diffs; `0 Critical / 0 Required`; owner acceptance riêng. Cập nhật statuses chín candidates/hai unsplit theo evidence; delivery vẫn theo quyền riêng. |

Mỗi checkpoint là review boundary, không mặc định một commit hay một model run. Corrections dùng commit mới khi được authorize; không amend/squash/rebase. Finding chưa giải quyết chặn acceptance, không chặn việc thu thêm evidence ở các case độc lập. Không tự triển khai production correction từ case response.

## Exact evaluation, reuse và correction flow

Contract này thay canary semantic-pass gate và stop-on-budget procedure của bản trước. Giữ B, CP0 raw-byte snapshot, runtime, immutable evidence và accepted-success reuse. CP1/CP2/C_fix_1 đã thực hiện giữ lịch sử; không bắt làm lại các checkpoints đó.

Runtime giữ `gpt-5.6-sol / medium`, sandbox `read-only`, ephemeral, ignore user config/rules, timeout `120000 ms`, concurrency/cap `2` không vượt local cap; frozen `max_attempts=2`, automatic retry `0`. Call allowance thuộc exact granted scope và actual dispatch ledger, không suy từ theoretical ceiling `132`.

### Prepare chỉ kiểm tra readiness

Prepare kiểm tra input inventory/provenance/exact bytes/schema/rubric membership/dependencies/options/budget, materialize package và công bố revision; dispatch 0. Nó không chứng minh reader hiểu đúng routing, DB safety hoặc reporting. Known malformed/missing input phải được giải quyết trước live; những lỗi hành vi chỉ observation mới xác định được thuộc live evaluation và correction loop.

### Canary kiểm tra backend; live readers kiểm tra nội dung

Canary là một số ít live calls để xác nhận backend Codex/OpenAI và đường dispatch/response/artifact hoạt động với package/runtime đã prepare. Thành công của canary là operational readiness, không yêu cầu response đạt rubric hoặc tài liệu hết lỗi. Nếu canary có semantic finding thì giữ observation và tiếp tục independent evaluation; chỉ operational/integrity failure ảnh hưởng đường chạy mới cần pause, điều tra và resume có evidence. Không lặp canary đến semantic pass trước khi cho các readers khác chạy. Evidence transport thành công đã có của current run đủ cho bước này khi runtime/package path không thay đổi; không thêm call chỉ để đổi nhãn canary.

Tính chính xác của bộ tài liệu phải được đánh giá qua nhiều independent fresh readers/cases trong live scope, paired evaluator proposals và main review. Đọc → phát hiện vấn đề → nhận định có bằng chứng → correction hẹp → chạy lại affected scope → kiểm tra lại; lặp trong quyền và budget đã cấp. Semantic acceptance chỉ đến sau khi đủ coverage và mọi material finding được giải quyết. Canary outputs vẫn góp evidence cho case tương ứng, không phải chứng nhận cả bộ tài liệu.

### Chạy lượt đánh giá và tiếp tục sau pause

- Fresh run dùng đủ 22 cases: 44 readers + 22 evaluators = 66 initial dispatch, không chọn một case phải semantic pass trước 21 case khác. Current run đã có 5 historical calls; dùng persisted state để xác định remaining work, không tạo lại run chỉ để bỏ canary gate.
- Chạy hết pending dependency-ready units còn lifetime budget trong phạm vi đã cấp. Semantic failure là finding để xử lý sau lượt; failed/budget-exhausted unit và dependent evaluator không làm mất hoặc chặn independent success/work. Không automatic retry failed/unknown để cố lấy pass.
- “Tạm skip” là disposition của operator ledger, không thêm persisted status: giữ succeeded/failed/pending và derived attempt_budget_exhausted/dependency_not_ready đúng như harness. Succeeded ở ordinal 2 vẫn là usable execution evidence nếu exact-current; chỉ khi cần attempt mới thì ceiling 2/2 chặn unit đó. Semantic pass/fail được ghi riêng.
- Nếu cần thận trọng, có thể pause ngay sau phát hiện vấn đề. Ghi lý do, started/settled/unknown units và đủ resume context; để started workers settle theo coordinator, không kill/discard attempts để tạo fresh start. Không hứa immediate semantic stop giữa wave vì harness không tự adjudicate proposal.
- Trước resume kiểm tra persisted run/revision, CP0 bytes, candidate ref/options và remaining action/call budget. Dùng resume với cùng run ID; accepted successes được reuse, chỉ pending eligible work chạy. Không prepare revision mới khi input không đổi. Failed cần explicit retry đúng unit và grant còn hiệu lực; unknown/integrity/operational uncertainty giữ evidence và xử lý đúng boundary trước khi tiếp tục phần bị ảnh hưởng. Lỗi integrity khiến coordinator không thể đọc trustworthy state có thể chặn cả command; không vượt validation để tiếp tục.

Current continuation từ state đã đọc ngày 2026-09-04, khi exact execution scope được cấp:

~~~powershell
$runId = 'run-d2e1b1d1bcee4334ab28374cf6549b1b'
node .agents/scripts/run-skill-eval-cli.mjs status --run $runId
node .agents/scripts/run-skill-eval-cli.mjs resume --run $runId
node .agents/scripts/run-skill-eval-cli.mjs report --run $runId
~~~

Revision 2 hiện có 3 succeeded, 42 reader pending và 21 evaluator dependency-blocked. Với input không đổi, remaining forecast là 42 + 21 = 63 new dispatch, giữ ba accepted outputs; tổng kể cả 5 historical calls là 68, không gọi tổng đó là initial ceiling 66. Các grants cũ có điều kiện canary; workflow revision không tự biến chúng thành unbounded remaining-call authority. Không chạy thêm trong lượt sửa docs này.

### Tổng hợp, correction và affected rerun

1. Khi không còn runnable work trong lượt/scope, tổng hợp mọi case: completed observations, semantic findings, execution failure, budget-exhausted/dependency-blocked, not-run và uncertainty. “Đã chạy hết phần có thể chạy” không phải tất cả cases pass. Một pause sớm cũng phải xuất cùng report cho phần đã quan sát và exact remaining work.
2. Main review đối chiếu raw response + context + rubric; phân biệt skill defect, evaluator disagreement, coverage/context defect và tooling defect. Với mỗi finding ghi bằng chứng, mức ảnh hưởng, proposed minimal diff, affected units/downstream closure, reuse candidates, remaining ordinals và estimate calls. Không kết luận thiếu coverage chỉ vì reader trả lời sai.
3. Sau khi correction scope/action được cấp, sửa hẹp, review và tạo immutable candidate theo commit authority. Không sửa suite để chiều candidate hoặc xóa observation cũ. Một instruction có thể cấp cả bounded correction/review/commit/rerun/report loop; không yêu cầu lại permission chỉ vì đến bước tiếp theo trong loop đã cấp.
4. Same-run prepare --run với B, corrected candidate ref và exact CP0 control-plane bytes; không truyền lại process settings. Kiểm tra generated manifests và actual reuse/invalidation trước dispatch. Run/resume tiếp mọi affected pending eligible unit còn budget; patch-check chỉ dùng khi muốn chọn bounded diagnostic closure, không làm semantic gate cho independent remainder. Không đưa budget-exhausted ID vào patch-check multi-selection vì validation all-or-nothing sẽ reject cả selection.
5. Full-bundle input khiến sửa core/reference có thể invalidate cả 22 candidate readers + 22 evaluators; đó là actual affected scope, không được gọi 21 case còn lại là unaffected chỉ vì diff ngắn. Baseline readers đúng fingerprint vẫn reuse. Unit hết budget được giữ pending resolution, tiếp tục phần affected khác còn runnable, rồi report kết quả và đề xuất bước xử lý tiếp.

### Unit đã hết budget và evidence qua các lượt

Frozen max_attempts=2 không reset qua revision; không sửa store, tăng trần hoặc copy accepted outputs sang run khác. Sau khi phần còn lại đã chạy, nếu cần đánh giá lại unit hết budget, phương án hẹp là prepare follow-up run với đúng B/final candidate và frozen control plane, rồi dispatch chỉ selected case/dependency closure qua patch-check. Current harness không có cross-run success import: một comparison case trong follow-up run cần baseline reader + candidate reader + evaluator, tức 3 calls khi cả ba chưa có evidence ở run đó. Prepare vẫn tạo đủ static scope; không vì vậy phải dispatch toàn bộ 66 units. Exact run/closure/call grant là phần của phương án xử lý sau report, chưa được thi hành ở planning revision này.

Các reports từng run giữ nguyên partial/mixed/incomplete truth; không ghép outputs rời rạc thành evaluator graph hoặc sửa report để giả whole-run completion. Final reviewer ledger có thể tham chiếu coherent per-case graphs từ các runs khác nhau, nhưng mỗi case phải có paired baseline/candidate và evaluator trong một graph do chính run đó xác minh, cùng B/final candidate bundle bytes, CP0 suite/context bytes và runtime contract. Case có evidence candidate cũ hoặc chỉ retained_reference chưa đáp ứng final candidate vẫn chưa được nghiệm thu. Không đòi một single-run report 22 current nếu điều đó buộc full re-execution chỉ vì một exhausted unit; phải trình bày per-run incomplete counts và 22-case coverage ledger rõ ràng cho owner acceptance. Không thêm automatic cross-run aggregator/schema vào PR6.

### Report và acceptance gate

Mỗi pause/end-of-pass report phải có: exact run/revision/refs/control snapshot, command và exit, dispatched/reused/invalidated unit IDs, attempts đã dùng/còn lại, output/proposal/report hashes, semantic findings với citations, pending/skipped reasons và affected dependencies, phương án correction cùng exact resume/rerun commands và call estimate. Không đủ evidence thì ghi unknown/not assessed, không ghi pass.

Report --run read-only, dispatch 0, authority advisory-only. Exit 1/incomplete vì failed/unknown/budget hoặc unrun cases không phủ nhận independent results đã có. CLI_REPORT_COVERAGE_INVALID exit 3 không phát report và không mutate. Final acceptance chỉ khi đủ 22 coherent current case graphs cho final candidate, mọi material finding/veto được adjudicate và giải quyết; semantic failure/budget skip là acceptance blocker, không phải default global execution stop. Nếu cùng một run đã đủ current evidence, same-ref prepare có thể normalize exact_current với zero dispatch; không rerun model chỉ để đổi coverage mode. Nếu dùng follow-up selected runs, dùng per-case coverage ledger ở trên và giữ nguyên report limitations.

## Review, evidence và acceptance

Latest pilot raw files đã đọc lại ngày `2026-09-04`; observation hash `e729aff901490b8f12edf4b79319542c942605cf918658d1298fd8c2724e00a6`, proposal hash `89548919d3af4979745cfdb053f93fc5dc4cd9f96b4734182c02df9b2a172230` khớp committed handoff. Response thật dùng `label/created_at/id` tie-break và thiếu explicit restore authorization/reconciliation; finding có căn cứ trong output. Không suy ra việc sửa skill chắc chắn chữa mọi response, không xác nhận production DB đang corrupt. Baseline/candidate evaluation mới vẫn phải được thực hiện.

Manual/main review ledger (transient, không tạo schema/tool mới) ghi mỗi case: run/revision, B/final candidate, reader/evaluator attempt IDs + output hashes, rubric/veto IDs, reviewer `case_status`/`comparison_status` theo governance eval-design, citations/rationale cho disagreement và disposition. Reviewer có thể đọc hidden rubric; reader không được nhận nó. CLI proposal dùng `assessment` và `comparison_findings`, không có human verdict/winner. Không ép advisory proposal thành `human_evaluation` hoặc rewrite raw outputs. Owner acceptance ghi trong owner brief, bind final ref/report hash + ledger/scope, không biến main review thành owner decision.

Acceptance phải đồng thời đạt:

1. Structural mapping đúng, core đọc riêng vẫn quyết định được permission/routing/stop/report; ba references có consumer/skip group thật, exact link, không mất invariant. CP2 chỉ chứa semantic delta đã duyệt.
2. Tất cả 22 **candidate** cases có đủ current paired evidence, mọi material criterion đạt qua reviewer adjudication, không confirmed safety veto, không `partially_passed`, `failed`, `not_run` hoặc material `inconclusive`. Baseline không có future-reference obligations. Existing baseline defect không cho phép candidate giữ defect đó.
3. Comparison không `regressed`; intentional semantic improvement được label riêng. Không claim final B/C_eval là bằng chứng causal cho riêng structural split. Advisory veto chưa giải quyết là blocker; không rerun cho tới pass hoặc sửa suite sau failure.
4. Main cumulative review `0 Critical / 0 Required`, không missing mandatory semantic/manual review. `Approved` chỉ là review verdict; owner phải chấp nhận migration riêng.
5. Final program table đối chiếu evidence của 9 candidates: `frontend-design`; `frontend-workflow`; `test-quality-strategy`; `nextjs-server-action-zod`; `implementation-planning-and-pr-breakdown`; `code-review-and-quality`; `git-checkpoint-workflow`; `github-pr-ci-workflow`; `supabase-safe-migration`. Hai unsplit decisions vẫn đúng: `code-commenting-and-maintainability` single-file, `maintain-repo-skills` giữ existing bundle. Phân biệt semantic acceptance, committed, pushed, PR open và merged; program delivery không complete trước actual PR6 merge.

Exact supplied evidence derive từ accepted producing `stdin.txt` và manifest path/hash; giữ `available`, `supplied`, `read`, `unknown` riêng. Whole bundle supplied nên không đặt context-reduction làm acceptance requirement của PR6. Không claim selective loading, runtime isolation/credential/network enforcement, native activation, token saving hoặc production readiness. Lưu immutable stdout snapshots với hashes ngoài repo; không ghi raw workspace/observations/transcripts/absolute temp paths vào Git. Temporary evidence không có durable-retention guarantee: nếu mất trước review/acceptance thì report gap và xin exact recovery/rerun authority, không giả lập artifacts.

## Kiểm tra, stop và rollback

Sau mỗi skill diff: `node .agents/scripts/validate-skill.mjs`, `node .agents/scripts/run-skill-evals.mjs validate --skill supabase-safe-migration`, `node .agents/scripts/run-skill-evals.mjs validate --all`, section-move/core/routing/link audit và `git diff --check`. Trước first live: chạy một lần `node --test .agents/scripts/run-skill-eval-cli.test.mjs` trên frozen harness; không chạy lại cho docs-only correction nếu source không đổi. Không lặp v1/structural unit suite, app build, lint toàn repo, Supabase reset/integration/E2E vì không đổi các implementation đó. Existing CI đã có CLI test; không sửa workflow.

Pause phần bị ảnh hưởng khi pins/control plane thay đổi, coverage không đủ để diễn giải evidence, unsupported runtime, missing action grant, unknown/corrupt evidence hoặc cần sửa ngoài skill/docs scope. Semantic finding và unit hết budget đi vào end-of-pass correction report; tiếp tục independent runnable work trong quyền đã cấp theo continuation contract. Global stop chỉ khi coordinator/input/runtime/authority không còn cho phép thực thi đáng tin cậy. Distinguish skill defect, coverage/context defect, tooling defect và evaluator disagreement trước chọn correction owner. Coverage/tooling correction phải được review riêng; giữ prior evidence, update plan/brief nếu material contract đổi. Không "giải quyết" bằng nới veto, nâng budget trong run hoặc relabel history.

Rollback chỉ skill core/ba references và owning status docs dưới quyền được cấp; giữ suites/harness/earlier waves/evidence. Revert semantic delta riêng nếu cần, không undo structural checkpoint vô cớ; bất kỳ rollback nào đổi candidate bytes đều cần revision/evidence currentness mới. Không xóa nhánh, reset history, sửa DB hoặc xóa raw evidence theo plan này.

## Bằng chứng planning và self-review

Workflow correction verification ngày `2026-09-04`: read-only current-run status dispatch `0/0/0`; source scheduler chỉ chọn pending/dependency-ready/budget-eligible work và giữ accepted success. Năm focused fake-worker regressions về resume, failure isolation, downstream retry và exhausted-budget reporting đạt `5/5`; không installed Codex/model call. Main reconciliation review đã loại canary semantic-pass/global-budget stop và single-run-completion requirement gây full re-execution; giữ missing/inconclusive cases blocking acceptance, không sửa attempt store/suite/harness. `git diff --check` pass; scope chỉ plan/owner brief/progress. Follow-up selected-run handling là phương án được báo trước execution, không một live action đã chạy.

Phần này giữ lịch sử planning trước owner instruction triển khai; trạng thái hiện tại ở đầu tài liệu và checkpoint ledger dưới đây supersede các claim permission/completion cũ.

- Đã fetch/prune, sync main fast-forward và tạo nhánh; verified ancestry/Git blob provenance và control-plane consumer, CLI source/parser/report/reuse contracts, roadmap/master/progress/handoff và latest raw pilot hashes. Claim ban đầu “pins đã verified” chưa chứng minh working-tree bytes giống B hoặc hoàn thành CP0 raw-byte freeze; được correction bên dưới thay thế.
- Validate target đạt `1 / 3 / 22 / 0 / 0`; catalog `9 / 27 / 187 / 0 / 0`; structural `11 skills / 0 errors / 0 warnings`; CLI `--help` pass.
- Zero-dispatch rehearsal B/B: `run-775b3ee348614a52b90f689443f8cea0`, workspace `ws-6ecc1ad83c794d1cb3afa895eab2085e`, revision 1, `44 readers / 22 evaluators / 66 units`, max ceiling `132`, concurrency `2`, local cap `8`, `max_attempts=2`. Prepare exit `0`, dispatch `0/0/0`. Đây chỉ là static packaging proof, không chứng minh raw bytes khớp B, không baseline observation/semantic pass; không dùng run rehearsal cho CP3.
- Planning diff gồm 7 Markdown files, không source/skill/suite/SQL change; UTF-8/no-BOM/final-newline/fence/conflict/added-secret checks, `25` added/new relative links/anchors và `git diff --check` pass; index không có staged file.
- CLI `96/96`, v1 `130/130` ở Stage 4 là historical execution evidence; scripts/CI byte-identical từ `0e92e93` tới B, không gọi các kết quả đó là test rerun của planning. Không model/live/database operation hoặc implementation mới.
- Initial main plan self-review `0 Critical / 0 Required` bị subsequent pinning review supersede: independently confirmed `1 Required`. Kiểm tra ngày `2026-09-04` thấy `core.autocrlf=true`, `23/23` raw files khác B, `0/23` khác sau chuẩn hóa duy nhất CRLF/LF. Existing rehearsal manifest khớp raw hashes `23/23`; `144` copied contexts/manifest entries của hai variants khớp expected bytes. Correction tách Git provenance khỏi exact CP0 snapshot, thêm pre/post-prepare và generated-manifest checks, không sửa harness/suite. CP0 snapshot cho future actual comparison vẫn chưa được freeze; evidence kiểm tra này không được relabel thành CP0 completion.
- Bounded correction re-review: `0 Critical / 0 Required` còn mở. Specialist/fresh-reader mới `not_run`; không thay bằng self-review. Scope/contract confidence `High`; behavioral readiness `Not assessed` cho candidate chưa tồn tại. Owner decision của whole plan vẫn `pending`; user đã authorize verify-and-correct đúng finding pinning này, không cấp skill implementation hoặc Git/live authority.
- Correction verification: raw SHA-256/byte count và control-plane manifest `23/23`, canonical evaluator suite bytes/inventory `3/3`, context copies/manifest entries `144/144`; in-memory controls xác nhận EOL-only pass provenance nhưng fail exact snapshot, còn non-EOL change fail provenance. Target validation `22 cases / 0 errors / 0 warnings`; correction chỉ đổi plan/owner brief/progress, suite/context bytes giữ nguyên, `git diff --check` pass. Chỉ đọc existing rehearsal, không prepare/run/model/database mới.

## CP0–CP1 local checkpoint — 2026-09-04

Snapshot trước commit; actual commit và CP2 được record ở ledger kế tiếp.

- Current owner instruction phê duyệt plan và local implementation, không thay material contract. Branch/HEAD/local `main`/cached `origin/main` đều khớp B; ba dependency ancestors khớp. Không fetch mới trong lượt này. Entry tree có đúng bảy planning docs chưa commit; giữ nguyên phần việc đó, chỉ reconcile current PR6 status ở owning docs.
- CP0 provenance đạt `23/23` sau chuẩn hóa duy nhất CRLF/LF. Snapshot raw bytes đã freeze tại thư mục `vocaspace-asm-pr6-cp0-BIJ3IB` dưới OS temp root; `snapshot.json` SHA-256 `3b5caa7fe5b40beb3fb728759357052f4a0280e003e9b269d387aeae804294a1`. Snapshot chứa sorted inventory, B blob IDs, raw SHA-256/byte count, đủ 22 case IDs; `raw/` giữ exact source copies. Đây là transient operator evidence, không phải harness artifact/schema; không commit raw files. Fresh session phải kiểm tra đúng hash/location này trước continuation, không re-freeze nếu mất hoặc drift.
- Đã audit đủ `11 regression + 6 routing + 5 fresh-reader`: existing-data/history, privilege/denied paths, Storage, trigger, lock/retry/idempotency, seed, verification truth, overlap, near-miss và core-only remote stop đều có criterion/consumer. Không phát hiện coverage gap hoặc semantic decision mới ngoài CP2; không đổi suite/context. Latest pilot observation/proposal hashes khớp handoff; raw response thực sự chọn `label/created_at/id` tie-break và thiếu explicit restore authorization/reconciliation. Chỉ xác nhận omission trong observation theo rubric, không quy kết production DB hoặc chứng minh root cause chung của model behavior.
- CP1 chỉ tách cấu trúc. Core giữ nguyên toàn bộ `RPC rules` vì danh sách ngắn đan xen permission/`SECURITY DEFINER`/`search_path`/state/idempotency minimum; reference RPC chứa trigger/concurrency procedure và RPC verification. Không tạo bản sao các rules đó. Mapping dưới đây là section audit so với B; mọi nội dung cũ giữ nguyên câu chữ.

| Section tại B | Vị trí CP1 | Lý do |
| --- | --- | --- |
| Frontmatter, activation, related skills, core rules, specialist signals, required workflow | Core nguyên văn | Quyết định scope, authority, inspection và stop trước hành động |
| Migration safety, file placement, seed data | `references/migration-and-seed.md` | Consumer migration/schema/backfill/seed; existing-behavior RLS/RPC-only có thể skip |
| RLS and permission rules | Procedure/examples trong `references/rls-and-storage.md`; hai câu hard-delete/helper-approval giữ core | Không giấu approval boundary trong reference |
| RPC rules | Core nguyên văn | Giữ nguyên ordered list chứa các security minimum, tránh tách hoặc lặp rule |
| Trigger rules | Procedure/test effect trong `references/rpc-trigger-concurrency.md`; toàn bộ `Do not` block giữ core | Managed-schema/provisioning scope phải biết trước khi hành động |
| Race conditions and idempotency | `references/rpc-trigger-concurrency.md` | Atomic-update/serialization procedure và examples; short-lock/no-external-call/idempotency minimum đã ở core |
| Storage policies | Procedure trong `references/rls-and-storage.md`; câu cấm broaden access giữ core | Access boundary luôn trong required context |
| Verification matrix | Migration/schema và seed → migration reference; RLS → RLS reference; RPC/trigger/race → RPC reference | Matching verification theo task; command discovery/reset/E2E/evidence truth vẫn core |
| Database-specific comments, anti-patterns, final checklist | Core nguyên văn | Reporting, reset, permission và safety checklist không optional |

- Core routing dùng đúng ba roadmap read conditions; overlap đọc mọi matching resource, SQL evidence không tự kích hoạt migration review, remote-push-only dùng core stop. Mọi reference link trực tiếp/contained; không nested link hoặc placeholder. Core đọc riêng xác định được permission, mandatory safety, related/reference routing, conflict/stop và reporting. Chỉ thêm heading/routing glue; semantic addition CP2 chưa có.
- Deterministic preservation audit: đủ `234` non-empty content lines của B ngoài headings/fences/separators vẫn có nguyên văn với multiplicity không giảm; manual section/diff review xác nhận grouping và scope không đổi. Core `236` lines, references `69/55/54` lines; đây chỉ là structure metrics, không claim token/context saving hoặc behavior equivalence. Transient `cp1-bundle-audit.json` giữ raw hashes của bốn files; chưa có immutable candidate ref/bundle manifest từ prepare.
- Verification: `node .agents/scripts/validate-skill.mjs` đạt `11 skills / 0 errors / 0 warnings`; `node .agents/scripts/run-skill-evals.mjs validate --skill supabase-safe-migration` đạt `1 / 3 / 22 / 0 / 0`; `validate --all` đạt `9 / 27 / 187 / 0 / 0`; diff/link/encoding/fence/conflict audit và `git diff --check` pass. Post-edit raw snapshot vẫn khớp `23/23`. Không prepare/dispatch mới; CLI test gate dành trước first live chưa chạy; không app tests/build/lint, DB reset/integration hoặc E2E vì không đổi implementation tương ứng.
- Main-agent formal structural review: `0 Critical / 0 Required`, confidence `High` cho CP1 contract preservation; đây không phải comparative semantic acceptance. Specialist/fresh-reader `not_run`, không dùng self-review thay evidence CP3. `C_struct`/`C_eval` chưa tồn tại; checkpoint dừng trước stage/commit theo Git skill và candidate-identity contract. Sau commit `C_struct` được owner authorize mới tiếp tục CP2 với diff riêng; không push/PR/merge/live/database action.

## CP2 local checkpoint — 2026-09-04

- Owner trả lời “có” cho local commit CP1 và tiếp tục CP2. Đã stage/review đúng tám files của checkpoint, commit `871dabcb34ab6125ccb75a6d2f2ced523a748c54` — `refactor(agent-skills): split Supabase migration procedures`. Đây là immutable `C_struct`, parent B; chưa push. Ba pre-existing handoff/index docs (`eval-design.md`, implementation-plans `README.md`, CLI-first owner brief) giữ unstaged ngoài commit. Grant CP1 đã consumed; chưa có `C_eval` hoặc quyền commit CP2.
- CP2 thêm đúng một core rule và một section năm bước trong `references/migration-and-seed.md`: xác định ba domain theo ADR/mutation/tests; validate current rows trước chọn correction; không tự tie-break khi business order mơ hồ; bảo toàn relative order/deleted metadata theo product contract; tách restore reconciliation và restore-conflict coverage thành mutation được authorize riêng. Không hard-code entity, numeric indexing hoặc SQL; không đổi routing, suite, harness hay database. Các rule/content CP1 còn nguyên.
- Main review đối chiếu exact CP2 contract, corrected suite rubric, ADR, mutation/read paths và integration-test source. Existing product evidence minh họa vì sao generic skill phải lấy contract theo domain; không biến contract của một entity thành quy tắc chung. Core-only review xác định được stop trước correction mơ hồ; reference không mở rộng backfill-only scope sang restore. `0 Critical / 0 Required`, confidence `High` cho bounded implementation; không còn material decision ngoài scope được suy đoán. Behavioral/fresh-reader/comparative acceptance vẫn `not_run`, không thay bằng self-review.
- Verification mới: structural validator `11/0/0`, target suites `1/3/22/0/0`, all suites `9/27/187/0/0`; `git diff --check`, link/encoding/fence/conflict audit pass. Post-commit/post-CP2 control plane vẫn khớp raw snapshot `23/23`; chỉ hai bundle files có semantic delta so với `C_struct`, hai references còn lại unchanged. Không chạy CLI/app/database tests vì không đổi implementation tương ứng; CLI suite vẫn là gate trước first live. Prepare/dispatch mới `0`.
- Tiếp theo cần local commit CP2 được owner authorize để tạo full immutable `C_eval`; sau đó mới prepare B/C_eval, kiểm tra exact packages/counts/manifests theo CP0 và xin exact live grant. Không dùng working-tree bytes hoặc floating `HEAD` thay candidate ref, không dùng grant CP1 cho CP2.

## CP3 prepare checkpoint — 2026-09-04

Runtime ledger correction trước first dispatch: `preflightCodexCli()` dùng Node `spawn("codex")` resolve tới `Programs/OpenAI/Codex/bin/codex.exe`, version `0.149.1`, SHA-256 `a395030b56b126f608f2403036dddb654a9c063213e9c2b5f85d954cf490ebe6`. Path/hash ban đầu lấy bằng PowerShell `Get-Command` nhận installation khác (báo `0.153.1`) nên không chứng minh executable của harness. Đã đối chiếu Node PATH resolution, bare và explicit executable version/help; actual prepared runtime/options vẫn `0.149.1`, không thay package hoặc harness. Giữ original evidence và thêm transient `cp3-cli-resolution-correction.json`; first mismatch dừng trước dispatch, run vẫn `0 attempts` lúc xác minh.

- Owner “duyệt” cấp một local commit CP2: `3def69126e8f8775ea17a21e3c3667a67e6c8263` — `feat(agent-skills): guard ordered soft-delete corrections`, sáu files đã stage/review, parent `C_struct`. Chưa push; không còn standing commit grant. Ba pre-existing handoff/index docs vẫn unstaged; post-prepare ledger docs cũng chưa commit.
- Đã chạy exact prepare template với B/C_eval full refs, concurrency/max-concurrency `2`, `max_attempts=2`: exit `0`, run `run-d2e1b1d1bcee4334ab28374cf6549b1b`, workspace `ws-2b3357fdeba0478992290ac988149aa3`, revision `1`, `44 readers + 22 evaluators = 66`, dispatch `0/0/0`. Lifetime ceiling `132` không phải grant. `status` exit `0`: `44 pending / 22 dependency_blocked`, failed/unknown/succeeded `0`; `report` exit `1`, `exact_current`, `0 current / 0 retained / 22 incomplete`, dispatch `0`.
- Exact source snapshot CP0 khớp trước/sau prepare và sau commit `23/23`; generated control plane khớp `23/23`, artifact inventory `330/330`, canonical evaluator suite copies `3/3`, prompts/policies `44/44`, repository context copies `110/110`, inline context copies `34/34` (tổng context entries `144/144`). Prepared reader stdin/schema hashes `44/44`, evaluator rubric/config `22/22` khớp frozen suites; existing loader revalidation pass. Không thêm planner prose hoặc answer key vào readers. Toàn bundle vẫn supplied, không claim selective loading/context saving.
- `workspace_input_hash = ec95531610306549beac34505b5b38e51ebe0211111e77900961aac3c5115fca`; execution-plan SHA-256 `266246994a3ab76e24680ed2e73fe730179bbaf37c3f2943b20c9ed85dccd516`. Canary `ssm-reg-additive-constraint-existing-data`: baseline unit `reader-96bfb0e1b1ae1dfb24a3cc967e8fe9f6b8df5a32f6601c4c0086cef4f50283fe`, candidate unit `reader-6a35d40b0c421b30c6cc5b8b3a824a9a4088d2dd82c455ad446f4e9b23b21fba`; closure evaluator `evaluator-88bb2406df44cb8a1a6dac4e7ae11d6614f85ea959c3eaea77fd0d9cadda459e`. Bundle manifest hashes baseline `b4c4e1867422673e7112f93afd5fdbe47f8d6629afeaa1972ca362a33969e48f`, candidate `22f23417bccd9d12ce8c3a619cb24be13f5b3889d4880bfb800e17858fc712aa`.
- Fresh pre-live gate: `node --test .agents/scripts/run-skill-eval-cli.test.mjs` đạt `96/96`, exit `0`, duration `22090.236 ms`; deterministic fake-worker tests, không real model call. CLI version/help preflight pass `codex-cli 0.149.1`, executable SHA-256 `56a84de2b617af6b95b0c5c5d8ae120d3c2fb69008ab330c7e7df3945b98b782`. Actual executable path/version/options và input hashes giữ trong transient ledger; trước live phải kiểm tra currentness. Prepared options đúng `gpt-5.6-sol / medium`, read-only/ephemeral/ignore-user-config/ignore-rules; runtime default timeout `120000 ms`, local cap `8`, owner cap `2`. Không chứng nhận runtime isolation hoặc DB safety từ các checks này.
- Transient evidence cùng CP0 folder `vocaspace-asm-pr6-cp0-BIJ3IB`: `cp3-prepare.stdout.json` SHA-256 `c0a8cc47c15ff0c1691828546c33acb948a684d913ba32c2b338535ce8b2a844`; `cp3-package-audit.json`, `cp3-cli-preflight.json`, `cp3-status.json`, `cp3-report.json`, và disclosure `cp3-live-package.md` chứa exact source inventory/hashes cùng commands. Evaluator input chỉ materialize sau accepted reader outputs, phải derive từ đúng frozen rubric + paired outputs; chưa có evaluator stdin hash để claim đã kiểm tra. Không commit raw packages hoặc absolute temp paths.
- Main preparation review: `0 Critical / 0 Required` cho static admission/package, confidence `High`; semantic readiness `Not assessed`. Xin quyền gửi exact frozen packages tới Codex/OpenAI và chạy canary `3` rồi remaining `63` chỉ khi canary adjudication đạt; tối đa `44 reader / 22 evaluator / 66 total`, automatic retry `0`, không standing correction/retry grant. Chưa có live grant nên không chạy `patch-check`, `run`, `resume` hoặc `retry`; không database/remote Git action.

## CP3 canary adjudication — 2026-09-04

- Owner “có” authorize gửi đúng disclosed packages tới Codex/OpenAI, initial ceiling `44 readers / 22 evaluators / 66 total`, concurrency `2`, retry `0`; remaining `63` conditional on canary. Đã chạy exact `patch-check` hai reader IDs ở prepare ledger, command exit `0`, dispatch `2/1/3`, cả ba ordinal `1`, failed/unknown `0`. Readers overlap `11:59:26Z–11:59:58Z`; candidate reader kết thúc `12:00:08.297Z`; evaluator bắt đầu sau đó `12:00:08.502Z`, kết thúc `12:00:34.710Z`. Không retry hoặc correction call. Persisted `process_metadata.cli_version` là `unknown`; version `0.149.1` là independent Node preflight evidence, không sửa raw metadata để giả thành worker attestation.
- Case duy nhất `ssm-reg-additive-constraint-existing-data`, revision `1`; exact producing attempt IDs là ba unit IDs trong prepare ledger với suffix `-attempt-1`. Accepted baseline observation SHA-256 `700748c3a398da7685f1b8f91ebf09e3573c4b5ea4cecfa513c8caa02e4fc582`; candidate `6dda0f0003f6f3a976d1cfb24e5bac4d4c0f2d4aed7d5feef9866d293ce60a0a`; evaluator proposal `6dbbb6d32af82865149965c42c72f35d4af59710983734ecff7205a65910d69a`. Raw evidence immutable, không rewrite hoặc relabel thành human artifacts.
- Evaluator advisory: `existing-data-safe-constraint-order = partially_satisfied`, `migration-breaks-or-corrupts-existing-data = not_triggered`. Main đọc cả hai raw responses và rubric: candidate cải thiện validation-before-correction, từ chối tie-break khi business order không xác lập được, giữ deleted metadata và tách restore; baseline vẫn đề xuất `order_index,label,created_at,id` tie-break không có ambiguous-data stop và thiếu restore boundary. Không dùng baseline defect để hạ candidate standard.
- **Required — routing evidence:** candidate raw response không nêu selection `references/migration-and-seed.md` hoặc skip hai reference còn lại như frozen expected_behavior yêu cầu. Full bundle supplied đã verified; không suy từ supplied sang selected/read. Chưa chứng minh đây là missing package hoặc causal defect của một câu skill. Phương án correction nhỏ cần owner duyệt: generic core reporting obligation cho reference selection/skip theo routing hiện có, không sửa suite sau failure.
- **Required — verification coverage:** candidate plan steps 6–7 nêu active contiguous validation/deleted metadata/partial unique/ambiguous-data cases, nhưng không xác lập table-wide `NOT NULL`/nonnegative revalidation cùng invalid-data coverage của criterion. Baseline có nhắc hậu kiểm NULL/số âm; vì có improvement và omission ở các chiều khác nhau, chưa đủ claim non-regression. Phương án correction: làm rõ verification phải bao phủ mọi preserved constraint trên đúng toàn bộ row scope và invalid-data cases, giữ generic skill và không đưa entity/SQL vào.
- Không nâng evaluator concern “duplicate stop chưa tường minh” thành blocker riêng: candidate step 3 chỉ cho compact unique/nonnegative indexes, step 4 và đoạn cuối dừng khi order chưa được xác lập; không yêu cầu literal wording chỉ để lấy pass. Evaluator nói không có skill/reference projection không chứng minh reader thiếu bundle; chính raw response thiếu selection evidence mới là finding có căn cứ.
- Main adjudication `candidate_case_status=partially_passed`, `comparison_status=inconclusive`, candidate confirmed veto `false`; baseline `failed` cho ambiguous tie-break proposal. Đây là main-agent advisory judgment, không owner acceptance; không kết luận production DB corrupt hoặc final migration regressed từ một case. Remaining `63` không chạy vì condition chưa đạt; không standing correction/retry grant và không tự dùng ordinal `2`.
- Post-canary `status` exit `0`: `3 succeeded / 42 pending / 21 dependency_blocked`, failure/unknown/running `0`. `report` exit `1`, `patch_check_mixed_revision`, `1 current / 0 retained / 21 incomplete`; SHA-256 `1d0395a6e6e24038cd90c98d5ace565cff582a4263ef1b2df608b974fb5f777a`. Cả hai đọc dispatch `0`, complete run tree `176/176` entries unchanged. Transient `cp3-canary-adjudication.json` SHA-256 `6c7967b68e891e2f8336e9678e8073c2e624e0e786dbb516b1307fcdc5655dc7` trong CP0 folder giữ citations/dispositions/hashes; `cp3-canary.stdout.json`, report/status/tree snapshots giữ execution evidence.
- Verdict CP3 hiện tại: `Changes required`, `0 Critical / 2 Required`, confidence `High` cho bounded evidence/stop decision; chưa whole-suite assessment. Chỉ cập nhật owning docs và transient evidence trong lượt live; không sửa skill/suite/harness/SQL, không commit/push/PR/merge/database action. Correction scope, new candidate commit/revision và exact correction live budget cần review/authorization trước khi tiếp tục.

## C_fix_1 correction review — 2026-09-04

- Exact owner workflow “correction -> review -> pass thì commit -> re canary -> report” được reconcile ở đầu plan trước implementation; scope là hai reporting/verification clarifications, không đổi frozen rubric hoặc acceptance. Re-canary chỉ cùng case/B/run, baseline success reuse, tối đa `1 candidate reader + 1 evaluator` calls ở ordinal `2`; không full wave hoặc additional retry/correction cycle.
- Core thêm mục `Reporting` độc lập: response nêu selected/skipped bundled references theo read conditions, hoặc core-only không có conditional reference. Không buộc đọc reference không match, không claim runtime read instrumentation. Đặt độc lập để review-only cũng áp dụng, không chỉ sau editing. Migration reference thêm explicit constraint inventory/full-row-scope verification và valid/invalid nullability/range/uniqueness coverage khi applicable; active-only ordering/uniqueness không thu hẹp table-wide constraint. Không entity, numeric-index hoặc SQL example mới; toàn bộ prior content còn nguyên.
- Main formal correction review trace hai findings → exact new rules → core-only/migration-only/overlap/near-miss consumers → frozen suite. `0 Critical / 0 Required` cho correction implementation, confidence `High`; không còn material scope/architecture/ownership/acceptance decision chưa giải quyết. Reviewer không gán lỗi chắc chắn cho model variance hoặc tuyên bố semantic pass trước re-canary. Hai omissions trong observation cũ vẫn giữ lịch sử; correction implementation pass chỉ mở gate local commit đã được owner cấp.
- Verification: structural validator `11/0/0`, target `1/3/22/0/0`, catalog `9/27/187/0/0`; diff/link/UTF-8/fence audit pass, CP0 snapshot `23/23` unchanged. CLI `96/96` từ pre-live vẫn current vì `.agents/scripts` unchanged; không chạy lại application/DB/integration hoặc model trước commit. `C_struct` và `C_eval` immutable; dùng new correction commit, không amend/squash. Commit gồm hai skill files và bốn owning PR6/status docs (kể cả prior canary evidence liên quan); ba pre-existing handoff/index docs vẫn ngoài checkpoint.

## C_fix_1 re-canary report — 2026-09-04

- Correction review đạt trước commit; đã tạo local commit `af732c235d0955f6dafb17718a0cbe34f858b01d` — `fix(agent-skills): clarify migration routing and verification evidence`, sáu files, không amend/push. Review này chỉ xác nhận implementation khớp approved correction, không phải semantic acceptance trước observation.
- Same-run prepare B/C_fix_1 exit `0`, revision `2`, workspace `ws-28eec955dbd844f89a6173632cb15277`, dispatch `0`; reused baseline canary, invalidated prior candidate/evaluator success. Frozen settings giữ nguyên, không truyền lại budget/concurrency. Workspace input hash `6f0b472bedcbe0db91479b27c4d5d456039f30d86dcb4ccd58adab87e5d72660`; plan SHA-256 `2e48fede249d066ed5a046c50a74ff8757cc4ef1d46a25ed1cbbe02ab4a84d2d`. Candidate stdin SHA-256 `165bee5425c1c40b5e26fd58deba38c37d252f428b9ed646e0243fe76b1edde4`; baseline stdin vẫn `7042b84e698f32a257cd68f6f37f018338b9582253e43d59b27d97e1283742ca`. Package audit: control plane `23`, inventory `330`, contexts `144`, reader inputs `44`, evaluator rubrics/config `22` đều khớp frozen source; revision 1 plan/ba outputs unchanged. Node bare/explicit CLI preflight vẫn `0.149.1` với corrected executable hash.
- Re-canary `patch-check` chỉ select candidate unit, closure reuse baseline: exit `0`, dispatch `1 reader / 1 evaluator / 2 total`, retry `0`, cả hai ordinal `2`, failures/unknown `0`. Candidate chạy `12:13:58.292Z–12:14:51.398Z`, evaluator `12:14:51.672Z–12:15:17.595Z`. Tổng run đến đây `3 readers + 2 evaluators = 5` calls; baseline giữ ordinal `1`. Không chạy remaining wave.
- Accepted candidate observation SHA-256 `fc485475f1f1dac3f22ffcf877f4d55c10e9ddf93bd9feef10740d5fed18df13`; evaluator proposal `1fadb32f4d7fe60fdca9e3ebb6be405a9fd66eed14b5a5834e0270af8ba0cf1c`. Producing IDs là canary candidate/evaluator IDs đã ghi với suffix `-attempt-2`. Advisory `partially_satisfied / not_triggered`; main đọc raw response, reused baseline và frozen rubric độc lập với proposal.
- Hai correction đã thể hiện trong output: có reference report, và finding 4/test item 8 nêu table-wide `NOT NULL`/nonnegative cùng đúng row scope/invalid-data coverage. Prior verification omission được đóng; prior routing omission được thay bằng actual routing error, không relabel canary cũ.
- **Required — chọn sai references:** đoạn cuối chọn cả migration/RPC/RLS, lý do tự suy từ SQL supplied rằng migration sẽ thay RPC và helper cần review. Frozen task/rubric chỉ yêu cầu migration reference, skip RPC/RLS; incidental implementation evidence không tự là requested change/review. Phương án correction tiếp theo cần owner decision: làm rõ trigger dựa trên task scope và phân biệt đọc SQL làm bằng chứng với review/change RPC/helper, không sửa frozen suite.
- **Required — deleted metadata recovery scope:** plan item 3 đề xuất khôi phục deleted `order_index` từ nguồn lịch sử; item 6 kiểm tra theo giá trị “đã được phê duyệt/khôi phục”, trong khi lệnh giữ nguyên deleted rows chỉ giới hạn ở bước compact. Response chưa giữ current hints trên toàn correction như criterion hoặc tách historical metadata recovery thành phạm vi authorize riêng. Không đủ căn cứ kết luận verified historical recovery tự nó corrupt dữ liệu; đây là unresolved scope/constraint preservation, không confirmed veto. Phương án cần owner duyệt: phân biệt backfill correction với separately authorized historical recovery, không nhầm với restore row và không triển khai product SQL.
- Không chấp nhận nguyên xi mọi evaluator claim: baseline raw không có restore boundary dù evaluator nói cả hai có; thiếu SQL/executed DB tests không phải lỗi của review/plan-only case; “chỉ advisory” không đủ chứng minh không có veto, nên main xét nội dung proposal; duplicate từ chối qua unambiguous-order guard/test đủ để không tạo wording-only blocker.
- Main verdict re-canary: `Changes required`, candidate `partially_passed`, comparison `inconclusive`, candidate confirmed veto `false`, `0 Critical / 2 Required`. Confidence `High` cho bounded evidence/stop decision, không whole-suite readiness. Candidate/evaluator đã dùng `2/2`; không thể chạy thêm canary trong run này và không tự tạo replacement run hoặc nâng budget. Current instruction kết thúc ở report, không cấp correction cycle tiếp theo hoặc remaining wave.
- Final `status` exit `0`: `3 succeeded / 42 pending / 21 dependency_blocked`, running/failed/unknown `0`. `report` exit `1`, `patch_check_mixed_revision`, `1 current / 0 retained / 21 incomplete`; SHA-256 `b594429b0a9fc28c840d98e37a611f5fc8d92893a98364020f5189a349998b02`. Status/report dispatch `0`, complete run tree `279/279` entries unchanged; mọi prior accepted output hash còn nguyên. Transient `fix1-adjudication.json` SHA-256 `90934273ac93c2ae05191414496f07664c0c579374667dc6e2edb6a6b31513b6` cùng `fix1-prepare.json`, `fix1-package-audit.json`, `fix1-canary.stdout.json`, final status/report/tree nằm trong CP0 folder. Post-run chỉ cập nhật docs, chưa commit; không database/remote Git action.

## Continuation end-of-pass — 2026-09-05

### Execution và evidence

- Contract checkpoint: `f663f349d9b817f3eb7ef03b73ba8f3b50874c60`; B `2be02df11e279b5c88f37d2fd609069a54c235ed`, C_fix_1 `af732c235d0955f6dafb17718a0cbe34f858b01d`. Run `run-d2e1b1d1bcee4334ab28374cf6549b1b`, revision 2, plan SHA-256 `2e48fede249d066ed5a046c50a74ff8757cc4ef1d46a25ed1cbbe02ab4a84d2d`; CP0 snapshot giữ nguyên 23 files/hash đã freeze, không prepare mới.
- Ba lần continuation dùng đúng `node .agents/scripts/run-skill-eval-cli.mjs resume --run run-d2e1b1d1bcee4334ab28374cf6549b1b`. Lần đầu sandbox chặn API (10013): hai timeout worker results là outcome_unknown; coordinator interrupt để dừng wave, thêm hai reader đã bắt đầu không có result. Lần hai ngoài sandbox thu observations tới khi owner yêu cầu pause vì hết quota: hai evaluator bị interrupt không có result. Hai command bị interrupt trả shell exit 1, không có complete command-summary JSON; dispatch phải truy từ persisted attempt records, không suy từ stdout trống. Không xóa/reset những attempts này.
- Owner ngày 2026-09-05 cho resume sau quota. Runtime/process absence/plan/snapshot đã revalidate; same-run resume reconcile hai interrupted evaluator thành `outcome_unknown / recovered_missing_result / coordinator_restart_without_result`, không tự retry. Command settle exit 1, `0 reader + 10 evaluator` dispatch, 46 reused, 0 invalidated. Tổng run 64 started attempts = 45 reader + 19 evaluator; 59 continuation (42 reader + 17 evaluator) cộng 5 historical. Đây là số worker attempts, không chứng minh số provider requests hoặc mọi call đều thành công. Harness retry 0; CLI transport đã tự reconnect khi lỗi mạng, không relabel thành zero transport retries.
- Final status exit 0: 56 succeeded (40 reader + 16 evaluator), 6 outcome_unknown (4 reader + 2 evaluator), 4 dependency-blocked evaluators; pending/running/failed/integrity/budget-blocked 0. Derived run `blocked / outcome_unknown` không phải global semantic stop. Canary candidate/evaluator vẫn succeeded ordinal 2/2; các attempt mới ordinal 1. Trong run có 62 units đã từng bắt đầu; bốn evaluator dependency-blocked chưa chạy.
- Report exit 1, `patch_check_mixed_revision`, 16 current/0 retained/6 incomplete; SHA-256 `7265d495e52212e8a30ebb86b9c803da67acff87b52fddf97fd22c348efa1951`. Status SHA-256 `2beffef742a2fb8ecfac06547f23d9c81bd237e5ebe54994038e58ef7e07e05f`. Hai commands dispatch 0 và trước/sau 651 file entries+hashes không đổi. Audit 64 attempt records, 58 structured outputs (gồm prior revision history) khớp hashes; CP0 23/23 unchanged. Không dùng same-ref prepare chỉ để đổi coverage mode khi còn missing evidence.
- Transient CP0 folder `vocaspace-asm-pr6-cp0-BIJ3IB`: `continuation-final-status.json`, `continuation-final-report.json`, `continuation-final-tree.json`, `continuation-attempt-audit.json`, `continuation-main-review.json`, `quota-resume.stdout.json` cùng grant/pause snapshots. Ledger main SHA-256 `aefdf1182c367b9574e31afdf7fa66f5e3ca2b171234be0302047f2dc00d8be5`. Ledger giữ exact unit/attempt IDs, producer revision, output/proposal hashes, raw-result paths, rationale và advisory cho từng case; không rewrite runner artifacts thành human_evaluation.

### Main review và dispositions

Đã đọc 40 current reader responses, 16 current evaluator proposals và rubric của đủ 22 cases; review raw source khi cần xác minh claim. Đây là main review, không specialist/fresh call thêm. Trên 16 coherent graphs: 8 passed, 7 partially_passed, 1 failed; sáu incomplete không có semantic case/comparison verdict. Không confirmed candidate safety veto trong 16 complete graphs; không suy thành toàn suite an toàn. Verdict `Changes required`, confidence `High` cho bounded evidence/stop disposition, final acceptance chưa đánh giá đủ.

- **R1 — Required, reference activation:** canary additive, local/remote, rollback, RPC security, trigger, route DB migration và fresh RPC cùng cho thấy chọn thêm RPC/RLS từ incidental supplied SQL hoặc chủ đề permission. Ví dụ candidate local/remote nói “migration thay thế RPC”; fresh RPC chọn RLS “cho boundary thực thi/quyền của RPC”. Frozen expected selection không có các routes đó. Correction nhỏ đề xuất: core phân biệt task yêu cầu change/review với SQL làm evidence cho mọi procedure, và phân biệt review RPC grants với review RLS/helper. Không cấm đọc context cần thiết, không sửa suite để chiều output. Rollback/trigger chỉ có candidate observation nên không gọi là complete comparative findings.
- **R2 — Required, deleted metadata:** giữ finding canary C_fix_1: response plan item 3 khôi phục deleted order_index lịch sử, item 6 kiểm theo giá trị approved/restored; preserve chỉ đặt trong compact step. Correction đề xuất giữ current deleted metadata trên toàn correction; historical recovery phải là scope được authorize riêng, khác restore row. Không khẳng định recovery có chứng cứ tự nó gây corrupt; đây là ambiguity/constraint-preservation blocker.
- **R3 — Required, browser fixture routing:** cả baseline/candidate `ssm-route-seed-browser-fixture` loại `frontend-workflow`, không fixture matrix. Candidate reference selection SSM đúng nhưng route nghiệp vụ sai; main xếp failed vì explicit exclusion, không tự dùng advisory partial làm verdict. Đề xuất clarify related-skill routing cho seed phục vụ authenticated browser QA và matrix actor/state/observable result, dùng owning frontend/test/planning procedure. Không quy đây là regression riêng do split vì baseline cùng lỗi.
- **R4 — Required, non-DB test documentation route:** cả baseline/candidate `ssm-route-nondb-zod-near-miss` thiếu `code-commenting-and-maintainability`; candidate loại SSM/references đúng. Root cause chưa đủ căn cứ gán cho SSM đang inactive. Đề xuất kiểm tra owning AGENTS/test-documentation route và consumer package trước khi chọn diff; không nhét non-DB global rule vào SSM hoặc thay frozen context trong PR6. Nếu cần đổi owning route/control plane ngoài scope thì phải là decision/checkpoint riêng, giữ hiện trạng case chưa pass.
- **R5 — Required, kết luận review/verification chưa đủ:** RPC security response chưa kết luận necessity của SECURITY DEFINER/returned-data scope; RLS response chưa review đủ mutation USING/WITH CHECK và ma trận allowed/denied tương ứng; trigger candidate chưa đề xuất effect/unaffected-data tests. Đề xuất report applicability/conclusion/planned coverage cho các invariant hiện đã nằm trong core/references, chỉ khi task trigger phù hợp; không biến mọi review thành yêu cầu chạy DB hoặc đọc mọi procedure. RPC current-state phải có state applicable thực, không tự thêm transition cho create-course case.
- **R6 — Required evidence gap, không phải skill defect:** bốn reader unknown làm bốn case thiếu paired dependencies, hai evaluator unknown làm hai case thiếu proposal. Đủ 22-case acceptance chưa thể thiết lập; không gọi chúng failed/semantic pass hay tự thêm skipped status.

Không chấp nhận các evaluator claims vượt evidence: review-only không bắt buộc thực thi DB tests; routing-only không phải tạo test header artifact; explicit routing tới owner phù hợp là subject. RLS proposal hạ điểm vì không chạy DB không được dùng làm blocker; giữ review/test-plan omission có căn cứ. Canary baseline không có restore boundary như proposal nói. Concurrency proposal nói baseline nêu external-call placement, fresh RPC proposal nói candidate nêu absence of external calls, nhưng raw không có câu tương ứng. Candidate concurrency nêu rõ điều đó nên main ghi bounded improvement; không extrapolate structural causality.

| Case | Coverage | Main candidate | Comparison | Findings |
| --- | --- | --- | --- | --- |
| `ssm-reg-additive-constraint-existing-data` | current | partially_passed | inconclusive | R1, R2 |
| `ssm-reg-concurrency-short-locks` | current | passed | improved | — |
| `ssm-reg-local-remote-authority` | current | partially_passed | inconclusive | R1 |
| `ssm-reg-published-migration-immutable` | current | passed | equivalent | — |
| `ssm-reg-retry-idempotency` | current | passed | equivalent | — |
| `ssm-reg-rls-role-denied-paths` | current | partially_passed | inconclusive | R5 |
| `ssm-reg-rollback-verification-truth` | incomplete | not assessed | not assessed | R1, R6 |
| `ssm-reg-rpc-security-search-path` | current | partially_passed | inconclusive | R1, R5 |
| `ssm-reg-seed-safety` | current | passed | equivalent | — |
| `ssm-reg-storage-policy-boundary` | incomplete | not assessed | not assessed | R6 |
| `ssm-reg-trigger-safety` | incomplete | not assessed | not assessed | R1, R5, R6 |
| `ssm-route-db-migration-and-tests` | current | partially_passed | inconclusive | R1 |
| `ssm-route-nondb-zod-near-miss` | current | partially_passed | equivalent | R4 |
| `ssm-route-remote-db-denied` | incomplete | not assessed | not assessed | R6 |
| `ssm-route-rls-storage-upload` | current | passed | equivalent | — |
| `ssm-route-rpc-validation-contract` | current | passed | equivalent | — |
| `ssm-route-seed-browser-fixture` | current | failed | equivalent | R3 |
| `ssm-fresh-migration-seed-overlap` | current | passed | improved | — |
| `ssm-fresh-remote-push-core-stop` | incomplete | not assessed | not assessed | R6 |
| `ssm-fresh-rls-storage` | incomplete | not assessed | not assessed | R6 |
| `ssm-fresh-rpc-trigger-concurrency` | current | partially_passed | inconclusive | R1 |
| `ssm-fresh-schema-only-skip-rls-rpc` | current | passed | equivalent | — |


### Phần chưa chạy và phương án tiếp theo (chưa được authorize)

- Current same-input resume không còn pending eligible work: không gọi lại để mong unknown thành success. `validateRetrySelection` trong `.agents/scripts/run-skill-eval-cli.mjs` chỉ nhận `failed`; `outcome_unknown` cũng không phải impact-eligible pending cho patch-check. Không sửa store, đổi status hay prepare revision để né boundary này.
- Nếu chỉ cần hoàn tất coverage của C_fix_1 hiện tại: propose một follow-up run B/C_fix_1/frozen control plane/runtime, dispatch đúng sáu case incomplete trong bảng. Mỗi selected case cần hai fresh readers + một evaluator trong chính graph của run mới: `12 readers + 6 evaluators = 18` calls, concurrency 2, retry 0. Prepare tạo static full scope nhưng patch-check chỉ chọn 12 reader units của sáu cases; không full 66 hoặc copy/import successes. Phương án này chưa giải quyết semantic findings hiện hữu.
- Khuyến nghị chốt correction scope R1–R5 trước lượt mới để không đánh giá lại candidate sắp thay. Với **SSM-only** correction làm đổi full-bundle fingerprint: logical affected scope là 22 candidate readers + 22 evaluators; baseline 20 successes reusable, hai baseline unknown giữ nguyên. Theo current state/CLI rebasing, dự kiến same-run eligible 19 candidate readers + 15 evaluators = 34 calls; canary 2/2 không rerun, hai candidate unknown và hai evaluator unknown giữ state, bốn dependent case vẫn thiếu graph. Bảy case (sáu incomplete hiện tại + canary) cần follow-up selected closure sau correction, 21 fresh calls. Tổng forecast 55, không phải grant và phải audit actual prepare/reuse/invalidation trước dispatch. Nếu R4 buộc đổi control plane/owner ngoài scope, forecast này không còn áp dụng và phải reconcile contract riêng.
- Exact operator flow sau khi có quyền và immutable candidate: current-run `prepare --run` theo template ở trên, rồi `resume --run run-d2e1b1d1bcee4334ab28374cf6549b1b`; follow-up `prepare --skill supabase-safe-migration --isolation synthetic --baseline-ref <B> --candidate-ref <approved-immutable-ref> --concurrency 2 --max-concurrency 2 --max-attempts 2`, đọc actual generated plan, chọn đúng hai reader IDs mỗi approved case bằng `patch-check --run <new-run-id> --unit <reader-id> ...`, cuối cùng `report --run <run-id>`. Placeholders không phải resolved IDs hoặc quyền thực thi; phải bind grant vào actual package/run/closure trước gọi model.
- Scope lượt hiện tại chỉ continuation/review/report: ba owning docs changed, không skill/suite/harness/application/SQL change, không DB tests/reset, không commit/push/PR. CLI tests trước đó không rerun vì harness unchanged. Commit message đề xuất cho reporting docs nếu owner duyệt: `docs(agent-skills): record PR6 continuation results and evidence gaps`.
