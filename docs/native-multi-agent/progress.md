# Native Multi-Agent Workflow — Progress

## Ownership

File này sở hữu trạng thái planning, implementation, verification và delivery hiện tại của native multi-agent feature. [Master Plan](./plan.md) sở hữu approved GOAL, semantic architecture, workstream decomposition, dependency order và phase gates; [Phase 2 detailed plan](./implementation-plans/phase-2/plan.md) sở hữu execution contract của Phase 2. Adaptive workflow [plan](../agent-workflow/plan.md) và [progress](../agent-workflow/progress.md) chỉ sở hữu adaptive program cùng compatibility boundary, không sở hữu trạng thái của feature này.

Chỉ ghi trạng thái có evidence thực tế. `approved`, implementation permission, `implemented`, `verified`, `committed`, `pushed`, `PR open` và `merged` là các trạng thái độc lập.

## Trạng thái chương trình

| Đơn vị | Master-program `approved` | Implementation permission | `implemented` | `verified` | `committed` | `pushed` | `PR open` | `merged` |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Phase 1 — NMA-WS1 | yes | consumed | yes | yes | yes | yes | yes | yes |
| CP1 — Relocation và durable ownership | yes | consumed | yes | yes; fresh review round 2 `PASS` | yes — `3e9d7d2` | no | no | no |
| NMA-WS2 — Routing và terminology | yes | consumed for candidate | yes; correction round 1 authored | yes; same-Reviewer round 1 `PASS` | yes — `36a08fd` | no | no | no |
| NMA-WS3 — Native orchestration skill | yes | consumed for candidate | yes; correction round 1 authored | yes; validator/tests/static matrix and same-Reviewer round 1 `PASS`; fresh-reader `not_run` with claim limit | yes — `36a08fd` | no | no | no |
| NMA-WS4 — Project role profiles | yes | consumed for candidate | yes | yes; TOML/config/static checks and bounded native smoke passed with stated limits | yes — `36a08fd` | no | no | no |
| Phase 2 aggregate | yes; exact detailed plan approved by owner-8 | consumed for current candidate, correction round 1 and remaining bounded smoke | yes | yes; same-Reviewer round 1 `PASS`, `0 Critical / 0 Required / 0 blocker` | yes — implementation `36a08fd`; completion record `bf599e8` | no | no | no |
| Phase 3 — NMA-WS5 | yes | no | no | no | no | no | no | no |
| Phase 4 — NMA-WS6 | yes | no | no | no | no | no | no | no |
| Phase 5 — NMA-WS7 | yes | no | no | no | no | no | no | no |

## CP1 — Relocation và durable ownership

- Owner Source Package: `owner_input_revision=6`; ordered refs `[owner-1, owner-2, owner-3, owner-4, owner-5, owner-6]`.
- Branch: `docs/native-multi-agent-phase-2`.
- Baseline trước checkpoint: `HEAD == main == origin/main == 62cf52879eff2cbd569b9ae43bee31e445a33c4f`; index sạch; ba planning artifacts dưới `docs/native-multi-agent/implementation-plans/` là untracked candidate đã được Main xác nhận thuộc task.
- Implemented: tracked Master Plan chuyển tới `docs/native-multi-agent/plan.md`; durable tracker này được tạo; adaptive plan/progress và implementation-plan index trỏ owner mới; `.gitignore` trỏ `/docs/native-multi-agent/reviews/`.
- Local review evidence: hai file được move byte-for-byte, giữ nguyên relative subtree dưới `docs/native-multi-agent/reviews/`; các SHA-256 là `cd0c985837f8a5242059baa44a0ad55259b8b00076a90aa32f9f42e9993186d5` và `cbab9ba4546ccd202998d563711259a7f759e57a14c1a0848fe0f5338323c0d1`. Chúng phải tiếp tục ignored, untracked và unstaged.
- Master Plan SHA-256: pre-edit `b189dad56f9860b6af2f3520ca1d18ccf567035b880254b293ea17e70504d005`; relocation candidate review round 0 `201d3b8bd430bd5ed527961095a5cf7cb8e5c1f4ce97bd8d780a1a6be06374d5`; correction round 1 `0320693b6181082f34ee2b05d2476375fbf5b6efb9a578b48e1742bab1c3b774`; correction round 2 `21b189c8e0a81f8e0606bccf60225f05bd5411228c0786b0df6bded9d3a244ba`.
- Deterministic verification: affected Markdown relative links, strict UTF-8, final newline, trailing whitespace, fence balance, old active links, review hashes/ignore boundary, old-root absence, candidate safety scan, `git diff --check`, branch/base/head và staged/tracked review audits đều pass. Các literal root cũ còn lại chỉ mô tả relocation đã được Owner yêu cầu hoặc giữ nguyên verbatim Owner source; không có active Markdown link trỏ root cũ.
- Fresh Master Plan Reviewer round 0: `BLOCKING_FINDINGS`, `0 Critical / 3 Required` (`RMR-001`, `RMR-002`, `RMR-003`). Correction round 1 chỉ sửa historical/current status và authority ownership, bind `owner_input_revision=6` cùng ordered refs, tăng Plan Revision `7 → 8` theo chính contract khi ownership detail đổi nhưng GOAL không đổi, và sửa Phase 2 handoff `revision 3 → revision 6`; không đổi GOAL/workstream semantics.
- Fresh Master Plan Reviewer round 1: `BLOCKING_FINDINGS`, `0 Critical / 1 Required` (residual `RMR-001`). Correction round 2 loại transient review/checkpoint/remote snapshot khỏi Master Plan và route toàn bộ current state sang file này cùng exact Owner source; Plan Revision giữ `8`, GOAL Revision giữ `1`, Owner package và workstream/phase semantics không đổi.
- Fresh Master Plan Reviewer round 2: `PASS`, `0 Critical / 0 Required`; `RMR-001`, `RMR-002`, `RMR-003` đều resolved trên exact `candidate_revision=plan-r9-relocation`. Master Plan SHA-256 tại verdict là `21b189c8e0a81f8e0606bccf60225f05bd5411228c0786b0df6bded9d3a244ba`; Phase 2 plan SHA-256 là `0137026a43e466236a530867c8302e00eafb4b088aa7a407deb9a026027a1800`.
- Reviewer-owned artifacts cho round 0, 1 và 2 dưới `docs/native-multi-agent/reviews/nma-001-phase-2/relocation-master-plan/` tiếp tục ignored, untracked, unstaged và nằm ngoài commit scope.
- Commit: `3e9d7d27504aabfa6e505e27798f6e1002226c5a` — `docs(native-multi-agent): relocate workflow documentation`.
- Remote: không push, không PR, không CI watch/fix, không merge, không deployment hoặc database/production mutation; các action này không được cấp quyền.

## Phase 2 — Operable native orchestration foundation

- Owner Source Package: `owner_input_revision=8`; ordered refs `[owner-1, owner-2, owner-3, owner-4, owner-5, owner-6, owner-7, owner-8]`. `owner-7` is an authority-only continuation through Phase 2 completion. `owner-8` verbatim `tiếp tục`, supplied directly after Main reported the exact remaining decision/action gate, approves the current Phase 2 detailed plan and authorizes the bounded tracked-clean candidate stability plus Planner/Implementor writer-enforcement smoke. GOAL/candidate semantics remain unchanged. Không có push, PR, merge, Phase 3/4, deployment, database/production/remote mutation, destructive action, history rewrite, force-push hoặc branch deletion authority.
- Candidate baseline/current HEAD: relocation commit `3e9d7d27504aabfa6e505e27798f6e1002226c5a` trên branch `docs/native-multi-agent-phase-2`; index chưa được stage trong implementation này.
- NMA-WS2 implemented: `AGENTS.md` thêm native-before-custom, ephemeral-before-persistent và exact skill route; `docs/agent-loops.md` thêm bốn mode cùng Main/Reviewer/Specialist boundary; active adaptive/planning/review owners dùng Specialist cho optional consultation và giữ Reviewer cho full lifecycle review. Historical implementation records không bị rewrite.
- NMA-WS3 implemented: `.agents/skills/native-multi-agent-workflow/SKILL.md` là canonical core owner; đúng ba resources có exact consumer/read condition cho Owner steer, review/reconciliation và verification. Core giữ authority, permission, Main-only transition, role/status, candidate/artifact, evidence và stop invariants; không thêm custom runtime, database, durable log, scheduler, harness, manifest hoặc fingerprint registry.
- NMA-WS4 implemented: `.codex/config.toml` là single owner của `default_subagent_model = "gpt-5.6-sol"`; bốn profiles `planner`, `reviewer`, `implementor`, `specialist` có exact identity/instructions, effort `high/high/medium/high` và sandbox `workspace-write/workspace-write/workspace-write/read-only`. Không thêm `agents.enabled`, concurrency hoặc audit key.
- Exact candidate scope: `AGENTS.md`; `docs/agent-loops.md`; `docs/agent-workflow/plan.md`; `docs/native-multi-agent/progress.md`; Phase 2 `plan.md` và `owner-review-brief.md`; planning skill core và `references/specialist-plan-review.md`; review skill core và `references/specialist-review.md`; new native skill core cùng đúng ba references; `.codex/config.toml` cùng bốn role profiles. Tổng cộng `10` tracked files modified và `9` files mới; index sạch, không file review nào tracked hoặc staged.
- Skill validation: `node .agents/scripts/validate-skill.mjs` trả `valid`, `12 skills`, `0 errors`, `0 warnings`; `node --test .agents/scripts/validate-skill.test.mjs` đạt `37/37`.
- Static contract matrix sau correction round 1: `STATUS_LITERAL_MATRIX=pass`; `BLOCKED(owner_input_unavailable)` chỉ áp dụng cho source missing/truncated/unreadable/reordered/coverage-incomplete, `BLOCKED(ambiguous_owner_intent)` áp dụng cho materially different semantic interpretations với Owner gate, và unsynchronized authority steer dùng `BLOCKED(owner_steer_not_synchronized)`. Bốn mode, Reviewer/Specialist distinction, năm Owner-change classes, review round `0/1/2`/exhaustion, blocker non-consumption, candidate/reviewer/artifact/stale blockers, no model fallback và no forbidden custom mechanism vẫn được validator/static sources bao phủ.
- Config evidence: bundled Python `tomllib` parse đạt `5/5` TOML files; `codex features list` chạy thành công từ repository trên `codex-cli 0.153.4` và ghi `multi_agent stable true`; `codex --strict-config doctor --json` ghi `config.load=ok`. Doctor overall exit `1` do sandbox không có auth/provider reachability và `TERM=dumb`, không phải config failure. System `python` shim và `bun` không available; hai attempt này không cung cấp verification evidence.
- Hygiene/scope sau correction round 1: đúng bảy tracked files nêu trong `P2R0-002` đã được chuẩn hóa thành CRLF thuần, không semantic edit; toàn bộ 11 correction/normalized paths đạt strict UTF-8, consistent EOL và final newline (`UTF8_EOL_FINAL_NEWLINE=pass files=11`); `git diff --check` đạt. Exact changed-path set vẫn là 17-file Phase 2 candidate, index rỗng. Repo-local review artifact vẫn ignored, untracked và unstaged. Occurrence `specialist reviewer` còn lại duy nhất nằm trong historical AW-PR3A record và được giữ nguyên theo scope.
- Historical native smoke attempt: lần admission đầu tiên trả `BLOCKED(profile_unavailable)` khi runtime chỉ báo generic identity `/root/reviewer`; không profile substitution, substantive review, candidate mutation hoặc review artifact write. Đây là evidence lịch sử của attempt đầu, không còn là current aggregate blocker.
- Current project-profile observations:
  - Planner: runtime nhận identity `Planner`, description `Creates or corrects an assigned Master Plan or detailed plan candidate for a managed VocaSpace workflow.`, developer constraints khớp `.codex/agents/planner.toml`, effort `high` và sandbox `workspace-write`. Main interrupt ngay sau spawn khi status là `running`, audit working tree/index/artifact không đổi, rồi same-session follow-up resume; Planner echo revision `7` cùng đủ bảy refs và xác nhận không write, delegation hoặc candidate inspection. Actual model name không được runtime surface độc lập.
  - Reviewer: runtime nhận project `Reviewer` instructions khớp `.codex/agents/reviewer.toml` và báo sandbox `workspace-write`; actual model name và reasoning effort không được child runtime surface độc lập. Same Reviewer session nhận authority refresh revision `7` sau audit idle/quiescent no-write, thực hiện cumulative review mà không mutate 17-file candidate, và chỉ ghi exact preassigned ignored artifact.
  - Implementor: runtime nhận identity `Implementor`, description `Implements only an accepted managed-workflow candidate and reports contract mismatch without rewriting the plan.`, developer constraints khớp `.codex/agents/implementor.toml`, effort `medium` và sandbox `workspace-write`; không candidate path được giao trong admission smoke nên không write. Actual model name không được runtime surface độc lập.
  - Specialist: project `Specialist` profile nhận bounded consultation, echo revision `7` cùng đủ bảy refs, giữ read-only/no-delegation. Initial package trả `Blocked` vì thiếu mandatory `verification-scenarios.md`; sau observed package-completeness change, same-session resume với đúng missing resource và không mở rộng khác, rồi trả advisory evidence rằng Reviewer/Specialist boundary nhất quán. Reviewer tự kiểm chứng và giữ full verdict ownership.
- Native-smoke claim limits: observed project-profile identity/constraints, Planner running interrupt, actual-state audit, same-session resume/steer propagation, Reviewer idle/quiescent authority refresh, exact artifact-only write, one-level Specialist blocker/resume/advice, Implementor mutation limited to its exact 11-path correction allowlist, và Planner mutation limited to exact `plan.md`/`owner-review-brief.md` assignment. Đây là positive role-compliance/writer-boundary evidence, không phải sandbox-enforced denial-path hoặc strict filesystem isolation. Evidence cũng không chứng minh malicious-agent resistance, interruption giữa protected write, runtime model identity, mọi client/config, future behavior hoặc production readiness. Tracked-clean candidate stability vẫn chờ exact admission.
- Cumulative implementation review round 0: exact artifact `docs/native-multi-agent/reviews/nma-001-phase-2/implementation-review/phase-2-candidate-r0-review-r0.md`, SHA-256 `a07189de5d56e1d28e6283f0cbe4ff346548608b67d1670d09a497c35014848e`; candidate `phase-2-candidate-r0` ổn định trên cả 17 paths; verdict `BLOCKING_FINDINGS`, `0 Critical / 3 Required`: `P2R0-001` status literals, `P2R0-002` mixed EOL trong bảy tracked files và `P2R0-003` stale progress. Same Implementor session đã author correction round 1 cho đúng ba finding; validator `valid` (`12` skills, `0` errors, `0` warnings), validator tests `37/37`, TOML parse `5/5`, status literal matrix, UTF-8/EOL/final-newline và `git diff --check` đều đạt. `codex --strict-config doctor --json` tiếp tục có `config.load=ok` nhưng overall exit `1` do sandbox thiếu credentials/provider reachability và `TERM=dumb`; đây chỉ là config-load evidence. Completed correction budget vẫn `0/2` cho tới khi same Reviewer hoàn tất rereview round 1. Phase 2 chưa verified hoặc committed.
- Same Reviewer round 1 ban đầu ghi `BLOCKED(phase_gate_incomplete)` với `0 Critical / 0 Required` vì detailed-plan approval cùng tracked-clean/writer-boundary smoke chưa đủ; blocker không tiêu correction round. Sau owner-8 approval và bounded blocker resume, Reviewer cập nhật duy nhất exact artifact `docs/native-multi-agent/reviews/nma-001-phase-2/implementation-review/phase-2-candidate-r1-review-r1.md`, final SHA-256 `2c2be0c048f6c074a6a5c2ddbcf0aed953fd0d83622c2866707461bdf939f4fc`, và trả `PASS`, `0 Critical / 0 Required / 0 blocker`. Exact 19-file candidate giữ nguyên tại verdict; tracked-clean `docs/native-multi-agent/plan.md` vẫn sạch với SHA-256 `21b189c8e0a81f8e0606bccf60225f05bd5411228c0786b0df6bded9d3a244ba`; index rỗng; artifact ignored, untracked, unstaged. Completed correction rounds được admit là `1/2`.
- Owner-8 blocker resume: same Planner session ban đầu fail loud `BLOCKED(owner_input_unavailable)` và `PLAN_CONTRACT_MISMATCH` khi chưa nhận complete package/writer ownership. Sau Main cung cấp đủ tám verbatim refs và mở đúng hai planning-owned writable paths, same session cập nhật approval state trong Phase 2 `plan.md` cùng `owner-review-brief.md`, giữ nguyên semantics/exclusions và không sửa path ngoài assignment; focused UTF-8/EOL/final-newline cùng `git diff --check` đạt. Detailed-plan approval blocker đã resolved; Main đang giữ tracked-clean candidate probe read-only để same Reviewer admission.
- Fresh-reader governance check: `not_run`. Các profile/native observations là bounded runtime evidence, không phải fresh-reader evidence theo contract và không thay cumulative Reviewer verdict.
- Product/application tests, browser QA, Supabase/database, CI và deployment: `not applicable`; actual diff không chạm các domain này.
- Current commit/push/PR/merge: Phase 2 implementation checkpoint đã tạo tại `36a08fd9a3a28c4e6fdf62b8b05267e282222c19` — `feat(native-multi-agent): establish native orchestration foundation` với đúng 19 files, `370 insertions(+), 51 deletions(-)`; progress-only completion record đã tạo tại `bf599e8` — `docs(native-multi-agent): record phase 2 completion`. Không push, PR, merge hoặc remote mutation.

## Dependency gate hiện tại

`CP1 relocation commit 3e9d7d2 → NMA-WS2/WS3/WS4 implemented → static/config checks → historical BLOCKED(profile_unavailable) attempt → current Planner/Reviewer/Implementor/Specialist bounded profile observations → cumulative review round 0 BLOCKING_FINDINGS (P2R0-001/002/003) → correction round 1 authored and targeted checks passed → same-Reviewer round 1 confirms all findings resolved → owner-8 approves exact detailed plan and bounded remaining smoke → Planner/Implementor positive writer-boundary evidence plus tracked-clean candidate stability admitted → same-Reviewer round 1 PASS with 0 Critical / 0 Required / 0 blocker → implementation commit 36a08fd → completion record bf599e8 → Phase 2 complete`.

Phase 2 đã mở gate cho local checkpoint commit. Không bắt đầu Phase 3 hoặc Phase 4 nếu chưa có authority riêng.

## Post-Phase 2 review-efficiency refinement

- Observed problem: CP1 relocation cần hai correction rounds vì correction đầu chưa đóng toàn bộ cùng một current-status ownership invariant; Phase 2 implementation dùng một correction round, một non-consuming blocker resume, rồi phát sinh progress-only self-reference khiến phải có thêm correction commit `2c4e1c8` sau `bf599e8`.
- Owner-approved refinement: giữ nguyên Master Plan; cập nhật native workflow core cùng review-reconciliation procedure để yêu cầu deterministic pre-review closure, artifact-neutral finding materiality, root-cause correction với focused/full rereview boundary, và stable progress closure.
- Deterministic coverage: `node --test .agents/scripts/native-multi-agent-workflow.test.mjs` đạt `4/4`; `node .agents/scripts/validate-skill.mjs` trả `valid` với `12 skills`, `0 errors`, `0 warnings`; existing validator tests đạt `37/37`. Verification scenario matrix ghi cả success/failure routes và correction-budget effect. Đây là source-level contract evidence, không phải native smoke; fresh-reader `not_run` vì task không cấp một independent executor và exact behavior đã do Owner quyết định.
- Remote boundary: refinement không cấp hoặc thực hiện push, PR, merge hay remote mutation; Git history tiếp tục sở hữu checkpoint identity.
