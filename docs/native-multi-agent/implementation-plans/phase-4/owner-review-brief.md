# Phase 4 — Owner review brief

Brief này là durable Owner decision/approval/action-authority interpretation surface cho [Phase 4 plan](./plan.md). Nó không phải dispatched Owner Source Package, inclusion/exclusion record, live lifecycle ledger, review journal hoặc future admission source. Main vẫn phải project exact package từ then-current Owner record trước mỗi managed-role dispatch.

## Current candidate source identity

- Workflow/phase/episode: `nma-phase4-20260912` / `Phase 4 — NMA-WS6` / `phase4-plan`.
- Current correction candidate: `phase4-plan-r2`.
- Candidate consumed Owner-record revision `3`, exact ordered refs `[owner-1, owner-2, owner-3]`. Identity này chỉ giúp diễn giải current plan candidate/review; later role không dùng nó để infer package membership hoặc authority.
- Brief này không sở hữu current role/session, review round/counter, artifact admission hoặc transition state và không preclaim `PASS`, implementation completion hoặc commit.

## Exact Owner decision evidence

### owner-1 — verbatim

> fetch remote, sync main, rồi thực hiện phase 4 native multi agent plan

Recorded effects only:

- Main đã fetch/prune, fast-forward `main` và tạo `feat/native-multi-agent-phase-4` tại `86bbb450c7e0f80551c9537c3c5a5b8d75278526` trước planning.
- Owner yêu cầu thực hiện approved Phase 4 / `NMA-WS6`; local planning, implementation và managed review nằm trong bounded Phase 4 scope.
- Entry không cấp push, PR, CI watch/fix, merge, deployment, database/production mutation, destructive action, history rewrite, force-push hoặc branch deletion.

### owner-2 — verbatim

> tiếp tục, nếu review pass thì có thể commit

Recorded effects only:

- Tiếp tục Phase 4 correction/implementation workflow hiện hành; không đổi GOAL, Master Plan, scope hoặc acceptance.
- Cấp local commit authority chỉ sau final admitted review `PASS`, cho exact reviewed Phase 4 scope và stable progress closure thuộc plan.
- Plan Reviewer `PASS` chỉ mở implementation route; nó chưa phải final implementation review `PASS` và chưa mở commit.
- Grant không cấp push, PR, CI watch/fix, merge, deployment, database/production/remote mutation, destructive action, history rewrite, force-push hoặc branch deletion.

### owner-3 — verbatim

> tiếp tục

Recorded effects only:

- Tiếp tục exact same bounded Planner correction; không tạo candidate lineage mới hoặc replacement session semantics.
- Entry không đổi findings, writer scope, GOAL, action authority hoặc remote/destructive boundary.

## Material interpretation for Owner review

1. Current `phase4-plan` author/review episode is bootstrap-only for exact source/session/candidate/artifact mechanics because it predates the CP2–CP5 reusable E2E contracts. It cannot count as post-contract prompt-independent Phase 4 pilot proof.
2. After CP2–CP5 and a reviewed core implementation checkpoint, a second fresh full E2E pilot uses a real remaining Phase-4-owned documentation slice: add Phase 4 plan/brief discovery to `implementation-plans/README.md` and record only observed current truth in `progress.md`.
3. Pilot payload supplies exact Owner source, authority, scope and repository routes, but not expected answers, review dimensions or seeded defects. Missing real correction or running-Implementor Owner steer remains `not_run`/`Partially verified`; neither is manufactured.
4. The plan adds a compact task-local lineage delta tying planning, review, native reconciliation, tests, profiles, brief/index and progress owners to exact gates and evidence/invalidation conditions.
5. Phase 5 remains excluded. No custom runtime, database, harness, scheduler, event store, fingerprint/provenance system or review oracle is introduced.

## Exact action permissions

| Action | Current interpretation |
| --- | --- |
| Correct these two Phase 4 planning candidates | Granted by Main writer package |
| Run deterministic author-side checks | Granted within bounded planning correction |
| Local Phase 4 implementation and managed review after admitted plan `PASS` | Granted by owner-1/current Main snapshot |
| Local commit | Granted by owner-2 only after final admitted review `PASS`; exact reviewed scope/stable progress closure only |
| Push / PR / CI watch-fix / merge / deploy / remote mutation | Not granted |
| Database/production mutation / destructive action / history rewrite / force-push / branch deletion | Not granted |
| Specialist consultation | `0`; no separate authority or unresolved qualifying cluster |

## Durable ownership boundary

- [Master Plan](../../plan.md) owns approved GOAL, `NMA-WS6`, dependency and Phase 4 gate.
- [Phase 4 plan](./plan.md) owns stable execution scope, lineage delta, acceptance, verification, pilot and stop contract.
- File này owns only material Owner decision/approval/action-authority interpretation plus minimum current candidate identity.
- [Progress](../../progress.md) owns concise observed current truth and stable completion evidence.
- Main's ephemeral workflow context owns exact future Owner package membership, role/session IDs, current authority snapshot, review rounds/counters, candidate bytes and artifact admission.
- Ignored Reviewer artifacts own detailed findings/verdicts for their exact rounds; neither this brief nor Main rewrites them.

## Decision boundary

No material GOAL or Master Plan change is introduced. Main must admit Plan Reviewer `PASS` for exact `phase4-plan-r2` before local implementation. Final admitted Implementation Reviewer `PASS` plus current mandatory native/manual evidence is required before owner-2 commit authority becomes usable. Exact role/session/round/artifact state remains in Main's ephemeral ledger and Reviewer artifacts; no remote or Phase 5 action is implied.
